// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: delivery_targets.sql

package sqlitegen

import (
	"context"
	"database/sql"
)

const countDeliveryTargetReportsForPrincipal = `-- name: CountDeliveryTargetReportsForPrincipal :one
SELECT COUNT(*) FROM delivery_target_reports
WHERE org_id = ? AND project_id = ? AND principal_id = ?
`

type CountDeliveryTargetReportsForPrincipalParams struct {
	OrgID       string
	ProjectID   string
	PrincipalID string
}

func (q *Queries) CountDeliveryTargetReportsForPrincipal(ctx context.Context, arg CountDeliveryTargetReportsForPrincipalParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, countDeliveryTargetReportsForPrincipal, arg.OrgID, arg.ProjectID, arg.PrincipalID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const deleteDeliveryTargetReport = `-- name: DeleteDeliveryTargetReport :execrows
DELETE FROM delivery_target_reports
WHERE org_id = ? AND project_id = ? AND environment_id = ? AND id = ?
`

type DeleteDeliveryTargetReportParams struct {
	OrgID         string
	ProjectID     string
	EnvironmentID string
	ID            string
}

func (q *Queries) DeleteDeliveryTargetReport(ctx context.Context, arg DeleteDeliveryTargetReportParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, deleteDeliveryTargetReport,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.ID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const getDeliveryTargetQuotaNotice = `-- name: GetDeliveryTargetQuotaNotice :one
SELECT principal_id, refused_at FROM delivery_target_quota_notices
WHERE org_id = ? AND project_id = ? AND principal_id = ?
`

type GetDeliveryTargetQuotaNoticeParams struct {
	OrgID       string
	ProjectID   string
	PrincipalID string
}

type GetDeliveryTargetQuotaNoticeRow struct {
	PrincipalID string
	RefusedAt   string
}

func (q *Queries) GetDeliveryTargetQuotaNotice(ctx context.Context, arg GetDeliveryTargetQuotaNoticeParams) (GetDeliveryTargetQuotaNoticeRow, error) {
	row := q.db.QueryRowContext(ctx, getDeliveryTargetQuotaNotice, arg.OrgID, arg.ProjectID, arg.PrincipalID)
	var i GetDeliveryTargetQuotaNoticeRow
	err := row.Scan(&i.PrincipalID, &i.RefusedAt)
	return i, err
}

const getDeliveryTargetReport = `-- name: GetDeliveryTargetReport :one

SELECT id, org_id, project_id, environment_id, principal_id, cluster_id, instance_uid,
       target_uid, namespace, name, vocabulary, generation, observed_generation,
       reported_at, received_at, report_interval_seconds, lifecycle, conditions,
       reporter, reporter_version, refusal_cause, refused_at, created_at
FROM delivery_target_reports
WHERE org_id = ? AND project_id = ? AND environment_id = ? AND principal_id = ?
  AND cluster_id = ? AND instance_uid = ? AND target_uid = ?
`

type GetDeliveryTargetReportParams struct {
	OrgID         string
	ProjectID     string
	EnvironmentID string
	PrincipalID   string
	ClusterID     string
	InstanceUid   string
	TargetUid     string
}

// Delivery-target condition reporting (#788, k8s-condition-reporting ADR).
func (q *Queries) GetDeliveryTargetReport(ctx context.Context, arg GetDeliveryTargetReportParams) (DeliveryTargetReport, error) {
	row := q.db.QueryRowContext(ctx, getDeliveryTargetReport,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.PrincipalID,
		arg.ClusterID,
		arg.InstanceUid,
		arg.TargetUid,
	)
	var i DeliveryTargetReport
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.EnvironmentID,
		&i.PrincipalID,
		&i.ClusterID,
		&i.InstanceUid,
		&i.TargetUid,
		&i.Namespace,
		&i.Name,
		&i.Vocabulary,
		&i.Generation,
		&i.ObservedGeneration,
		&i.ReportedAt,
		&i.ReceivedAt,
		&i.ReportIntervalSeconds,
		&i.Lifecycle,
		&i.Conditions,
		&i.Reporter,
		&i.ReporterVersion,
		&i.RefusalCause,
		&i.RefusedAt,
		&i.CreatedAt,
	)
	return i, err
}

const insertDeliveryTargetQuotaNotice = `-- name: InsertDeliveryTargetQuotaNotice :exec
INSERT INTO delivery_target_quota_notices (principal_id, org_id, project_id, refused_at)
VALUES (?, ?, ?, ?)
`

type InsertDeliveryTargetQuotaNoticeParams struct {
	PrincipalID string
	OrgID       string
	ProjectID   string
	RefusedAt   string
}

func (q *Queries) InsertDeliveryTargetQuotaNotice(ctx context.Context, arg InsertDeliveryTargetQuotaNoticeParams) error {
	_, err := q.db.ExecContext(ctx, insertDeliveryTargetQuotaNotice,
		arg.PrincipalID,
		arg.OrgID,
		arg.ProjectID,
		arg.RefusedAt,
	)
	return err
}

const insertDeliveryTargetReport = `-- name: InsertDeliveryTargetReport :exec
INSERT INTO delivery_target_reports (
    id, org_id, project_id, environment_id, principal_id, cluster_id, instance_uid,
    target_uid, namespace, name, vocabulary, generation, observed_generation,
    reported_at, received_at, report_interval_seconds, lifecycle, conditions,
    reporter, reporter_version, created_at
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
`

type InsertDeliveryTargetReportParams struct {
	ID                    string
	OrgID                 string
	ProjectID             string
	EnvironmentID         string
	PrincipalID           string
	ClusterID             string
	InstanceUid           string
	TargetUid             string
	Namespace             string
	Name                  string
	Vocabulary            int64
	Generation            int64
	ObservedGeneration    int64
	ReportedAt            string
	ReceivedAt            string
	ReportIntervalSeconds int64
	Lifecycle             string
	Conditions            string
	Reporter              string
	ReporterVersion       string
	CreatedAt             string
}

func (q *Queries) InsertDeliveryTargetReport(ctx context.Context, arg InsertDeliveryTargetReportParams) error {
	_, err := q.db.ExecContext(ctx, insertDeliveryTargetReport,
		arg.ID,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.PrincipalID,
		arg.ClusterID,
		arg.InstanceUid,
		arg.TargetUid,
		arg.Namespace,
		arg.Name,
		arg.Vocabulary,
		arg.Generation,
		arg.ObservedGeneration,
		arg.ReportedAt,
		arg.ReceivedAt,
		arg.ReportIntervalSeconds,
		arg.Lifecycle,
		arg.Conditions,
		arg.Reporter,
		arg.ReporterVersion,
		arg.CreatedAt,
	)
	return err
}

const lastDeliveryFetchAt = `-- name: LastDeliveryFetchAt :one
SELECT actor_id, occurred_at FROM audit_tenant_events
WHERE org_id = ? AND project_id = ? AND env_id = ? AND actor_id = ?
  AND type = ?
ORDER BY seq DESC
LIMIT 1
`

type LastDeliveryFetchAtParams struct {
	OrgID     string
	ProjectID sql.NullString
	EnvID     sql.NullString
	ActorID   sql.NullString
	Type      string
}

type LastDeliveryFetchAtRow struct {
	ActorID    sql.NullString
	OccurredAt string
}

// The server-observed layer (ADR D2): the last authenticated delivery fetch
// of one principal in one environment, from the audit trail.
func (q *Queries) LastDeliveryFetchAt(ctx context.Context, arg LastDeliveryFetchAtParams) (LastDeliveryFetchAtRow, error) {
	row := q.db.QueryRowContext(ctx, lastDeliveryFetchAt,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvID,
		arg.ActorID,
		arg.Type,
	)
	var i LastDeliveryFetchAtRow
	err := row.Scan(&i.ActorID, &i.OccurredAt)
	return i, err
}

const listDeliveryTargetReports = `-- name: ListDeliveryTargetReports :many
SELECT id, org_id, project_id, environment_id, principal_id, cluster_id, instance_uid,
       target_uid, namespace, name, vocabulary, generation, observed_generation,
       reported_at, received_at, report_interval_seconds, lifecycle, conditions,
       reporter, reporter_version, refusal_cause, refused_at, created_at
FROM delivery_target_reports
WHERE org_id = ? AND project_id = ? AND environment_id = ?
ORDER BY cluster_id, namespace, name, id
`

type ListDeliveryTargetReportsParams struct {
	OrgID         string
	ProjectID     string
	EnvironmentID string
}

func (q *Queries) ListDeliveryTargetReports(ctx context.Context, arg ListDeliveryTargetReportsParams) ([]DeliveryTargetReport, error) {
	rows, err := q.db.QueryContext(ctx, listDeliveryTargetReports, arg.OrgID, arg.ProjectID, arg.EnvironmentID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []DeliveryTargetReport
	for rows.Next() {
		var i DeliveryTargetReport
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.EnvironmentID,
			&i.PrincipalID,
			&i.ClusterID,
			&i.InstanceUid,
			&i.TargetUid,
			&i.Namespace,
			&i.Name,
			&i.Vocabulary,
			&i.Generation,
			&i.ObservedGeneration,
			&i.ReportedAt,
			&i.ReceivedAt,
			&i.ReportIntervalSeconds,
			&i.Lifecycle,
			&i.Conditions,
			&i.Reporter,
			&i.ReporterVersion,
			&i.RefusalCause,
			&i.RefusedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const purgeDeliveryTargetReport = `-- name: PurgeDeliveryTargetReport :execrows
DELETE FROM delivery_target_reports
WHERE id = ? AND received_at < ?
`

type PurgeDeliveryTargetReportParams struct {
	ID         string
	ReceivedAt string
}

// PurgeDeliveryTargetReport deletes one expired row, guarded by the same
// cutoff so a report accepted since the select keeps its row.
// hikyo:instance-scoped
func (q *Queries) PurgeDeliveryTargetReport(ctx context.Context, arg PurgeDeliveryTargetReportParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, purgeDeliveryTargetReport, arg.ID, arg.ReceivedAt)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const recordDeliveryTargetRefusal = `-- name: RecordDeliveryTargetRefusal :execrows
UPDATE delivery_target_reports
SET refusal_cause = ?, refused_at = ?
WHERE org_id = ? AND project_id = ? AND environment_id = ? AND id = ?
`

type RecordDeliveryTargetRefusalParams struct {
	RefusalCause  sql.NullString
	RefusedAt     sql.NullString
	OrgID         string
	ProjectID     string
	EnvironmentID string
	ID            string
}

func (q *Queries) RecordDeliveryTargetRefusal(ctx context.Context, arg RecordDeliveryTargetRefusalParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, recordDeliveryTargetRefusal,
		arg.RefusalCause,
		arg.RefusedAt,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.ID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const selectExpiredDeliveryTargetReports = `-- name: SelectExpiredDeliveryTargetReports :many
SELECT id, org_id, project_id, environment_id, principal_id, received_at
FROM delivery_target_reports
WHERE received_at < ?
ORDER BY received_at, id
LIMIT 100
`

type SelectExpiredDeliveryTargetReportsRow struct {
	ID            string
	OrgID         string
	ProjectID     string
	EnvironmentID string
	PrincipalID   string
	ReceivedAt    string
}

// SelectExpiredDeliveryTargetReports is one bounded installation-wide purge
// batch (ADR D6: no accepted report for 30 days). Repeated scheduler runs
// commit progress without an unbounded backlog inside one transaction.
// hikyo:instance-scoped
func (q *Queries) SelectExpiredDeliveryTargetReports(ctx context.Context, receivedAt string) ([]SelectExpiredDeliveryTargetReportsRow, error) {
	rows, err := q.db.QueryContext(ctx, selectExpiredDeliveryTargetReports, receivedAt)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SelectExpiredDeliveryTargetReportsRow
	for rows.Next() {
		var i SelectExpiredDeliveryTargetReportsRow
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.EnvironmentID,
			&i.PrincipalID,
			&i.ReceivedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateDeliveryTargetQuotaNotice = `-- name: UpdateDeliveryTargetQuotaNotice :execrows
UPDATE delivery_target_quota_notices SET refused_at = ?
WHERE org_id = ? AND project_id = ? AND principal_id = ?
`

type UpdateDeliveryTargetQuotaNoticeParams struct {
	RefusedAt   string
	OrgID       string
	ProjectID   string
	PrincipalID string
}

func (q *Queries) UpdateDeliveryTargetQuotaNotice(ctx context.Context, arg UpdateDeliveryTargetQuotaNoticeParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, updateDeliveryTargetQuotaNotice,
		arg.RefusedAt,
		arg.OrgID,
		arg.ProjectID,
		arg.PrincipalID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const updateDeliveryTargetReport = `-- name: UpdateDeliveryTargetReport :execrows
UPDATE delivery_target_reports
SET namespace = ?, name = ?, vocabulary = ?, generation = ?, observed_generation = ?,
    reported_at = ?, received_at = ?, report_interval_seconds = ?, lifecycle = ?,
    conditions = ?, reporter = ?, reporter_version = ?, refusal_cause = NULL, refused_at = NULL
WHERE org_id = ? AND project_id = ? AND environment_id = ? AND id = ?
`

type UpdateDeliveryTargetReportParams struct {
	Namespace             string
	Name                  string
	Vocabulary            int64
	Generation            int64
	ObservedGeneration    int64
	ReportedAt            string
	ReceivedAt            string
	ReportIntervalSeconds int64
	Lifecycle             string
	Conditions            string
	Reporter              string
	ReporterVersion       string
	OrgID                 string
	ProjectID             string
	EnvironmentID         string
	ID                    string
}

// An accepted report replaces the row's asserted state and clears any
// recorded refusal: the refusal is older than the last accepted report now.
func (q *Queries) UpdateDeliveryTargetReport(ctx context.Context, arg UpdateDeliveryTargetReportParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, updateDeliveryTargetReport,
		arg.Namespace,
		arg.Name,
		arg.Vocabulary,
		arg.Generation,
		arg.ObservedGeneration,
		arg.ReportedAt,
		arg.ReceivedAt,
		arg.ReportIntervalSeconds,
		arg.Lifecycle,
		arg.Conditions,
		arg.Reporter,
		arg.ReporterVersion,
		arg.OrgID,
		arg.ProjectID,
		arg.EnvironmentID,
		arg.ID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}
