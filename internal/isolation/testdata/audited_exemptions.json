{
  "_comment": "Name-pinned exemptions from the audit completeness invariant (audit-model ADR CI invariant 2). The ADR's default-deny rule refuses `audited: none` to unauthenticated- and system-class operations, but declares no event types for health probes, or local product/build prints; the demonstration org reads are #44 scaffolding whose real surface (#48) must map them. Every entry here is a reviewed deviation, not a hole: removing an entry without mapping the operation fails the build.",
  "wire": {
    "cli:about": "local product-information and build-metadata print: no server, no store",
    "cli:access": "client transport for the /grants routes at every depth, whose operations carry the audit mapping (grant.created / grant.modified / grant.revoked / grant.template_applied / grant.membership_read)",
    "cli:adapter": "client transport for the tenant-scoped deployment-adapter routes, whose operations carry the complete adapter.* audit mapping; the local browser handoff opens no separate server-side act",
    "cli:account": "client transport for POST /api/v1/auth/credential/establish, audited server-side as auth.credential_established / auth.credential_authority_refused",
    "cli:compose": "client transport for the tenant-scoped delivery routes (#63): GET .../delivery carries the delivery.fetch mapping (identity.delivery_fetched, disclosure.value_revealed on a delivering fetch) and POST .../delivery/offline-records carries identity.offline_records_reconciled. The verb writes local generations, stamps, snapshots and offline records — a client-side act on this box that reaches no separate server-side record beyond those the routes already carry",
    "cli:context": "entirely client-local: the trust store and named contexts live on this box, reach no server, and hold no credential material; the offline client-side audit record is the compose ticket's (#63)",
    "cli:definitions": "client transport for the tenant-scoped definitions export/check/plan/apply routes; their operations own the read exemptions and definitions plan/apply audit events, so the verb adds no separate auditable act",
    "cli:dynamic-provider": "client transport for the tenant-scoped dynamic-secret provider routes (#147), whose operations carry the dynamic.provider_* audit mapping; no separate server-side act",
    "cli:lease": "client transport for the environment-scoped lease routes (#147), whose operations carry the dynamic.lease_* audit mapping; the display-once mint discloses through the print triad on the operator's terminal, holding no separate server-side record",
    "cli:run": "client transport for the tenant-scoped delivery routes (#63): GET .../delivery carries the delivery.fetch mapping (identity.delivery_fetched, disclosure.value_revealed per delivered secret) and POST .../delivery/offline-records carries identity.offline_records_reconciled. run merges into the child environment and execs — it holds no server-side act of its own beyond the fetch the route audits",
    "cli:doctor": "client transport for GET /api/v1/instance/retention-health and /saml-providers; both server reads are audited and return the canonical health state",
    "cli:env": "client transport for the tenant-scoped environment routes, whose operations carry the audit mapping",
    "cli:folder": "client transport for the tenant-scoped folder routes, whose operations carry the audit mapping",
    "cli:instance-config": "client transport for the instance-scoped provider routes, whose operations carry the audit mapping",
    "cli:key": "client transport for the tenant-scoped key-catalogue routes, whose operations carry the audit mapping",
    "cli:import": "client transport for the tenant-scoped import routes (#68): phase 1 is a proof-scoped pure read (import.presence, audited-none like every other read of the same shape) and phase 2 is `hikyo values import`, which rides cli:values and whose operation carries the mapping (value.set per key plus value.imported per run). The verb itself authors local artifacts and adds transport, not an auditable act",
    "cli:login": "client transport for POST /api/v1/auth/local/login: the server audits the login it performs (auth.login, auth.session_created); the verb itself adds transport, not an auditable act",
    "cli:logout": "client transport for POST /api/v1/auth/logout, audited server-side as auth.logout",
    "cli:org": "client transport for the /api/v1/orgs routes, whose operations carry the audit mapping",
    "cli:pin": "client transport for the tenant-scoped pin routes, whose mutations and expiry refusals are audited server-side as pin.created, pin.reassigned, pin.renewed, pin.released, and pin.expiry_refused; the verb itself adds transport, not an auditable act",
    "cli:project": "client transport for the tenant-scoped project routes, whose operations carry the audit mapping",
    "cli:project-settings": "client transport for the environment settings routes, whose operations carry the audit mapping (settings.protected_flag_changed / settings.reauthentication_window_changed / auth.effective_window_lowered)",
    "cli:remote": "client transport for the /api/v1/instance/remotes routes, whose operations carry the audit mapping (remote.added / remote.renamed / remote.removed / remote.directory_viewed / remote.fetch_failed). The fingerprint confirmation and the credential paste are LOCAL acts on the operator's terminal, reaching no server and holding no server-side record to write",
    "cli:remote-credential": "client transport for the /api/v1/instance/connections routes, whose operations carry the audit mapping (remote.credential_minted / remote.credentials_listed / remote.credential_revoked)",
    "cli:revision": "client transport for the tenant-scoped revision-history routes (#51). Both are pure lineage reads whose operations are audited-none: history carries revision numbers, publishers, timestamps and which keys moved, never a value, so a trail entry would only duplicate the revision.published record the publish already wrote",
    "cli:rotate-scanning-key": "client transport for POST /api/v1/instance/rotate-scanning-key; the server audits the rotation it performs (crypto.scanning_key_rotated)",
    "cli:rotate-dek": "client transport for POST /api/v1/instance/rotate-dek; the server audits the rotation it performs (crypto.dek_rotated)",
    "cli:rotate-master-key": "client transport for POST /api/v1/instance/rotate-master-key; the server audits the rotation it performs (crypto.master_key_rotated)",
    "cli:rotate-root-key": "client transport for POST /api/v1/instance/rotate-root-key; the server audits each phase it performs (crypto.root_key_rotation_prepared / _verified / _finalized)",
    "cli:reencrypt": "client transport for the instance and project reencrypt routes; the server audits the walk it performs (crypto.reencrypt_completed)",
    "cli:rotate-token-key": "client transport for POST /api/v1/instance/rotate-token-key; the server audits the rotation it performs (crypto.token_key_rotated)",
    "cli:sa": "client transport for the project-scoped service-account and credential routes, whose operations carry the audit mapping (identity.service_account_created / identity.service_account_deleted / identity.credential_minted / identity.credential_revoked / identity.credentials_listed)",
    "cli:scim": "client transport for the /scim-bindings routes, whose operations carry the audit mapping (scim.binding_* / scim.mapping_* / scim.credential_* / scim.directory_read)",
    "cli:update": "client-local read and write of public Hikyo release metadata and the chosen local channel; it reaches no Hikyo server operation and holds no secret or tenant material",
    "cli:values": "client transport for the tenant-scoped value routes, whose operations carry the audit mapping",
    "cli:version": "local build-metadata print: no server, no store",
    "cli:welcome": "local first-contact and build-version print: no server, no store",
    "cli:whoami": "client transport for GET /api/v1/auth/whoami, which is itself exempt (it reports the session the caller already holds and writes nothing)",
    "http:GET /api/v1/auth/identities": "reports the caller's own linked identities and writes nothing; a self read whose subjects are already recorded at link/unlink time (auth.identity_linked / auth.identity_unlinked), so an event here would duplicate that once per poll, exactly like whoami",
    "http:GET /api/v1/auth/methods": "unauthenticated public discovery: the enabled provider slugs and whether local login is on, instance-level and never per-account; no principal, no tenant data, no state change, same class as GET /api/v1/meta",
    "http:GET /api/v1/orgs/{org}/projects/{project}/definitions/plans/{plan}": "reads an immutable definitions plan under definitions-edit authority and changes no state; plan creation and apply carry the auditable acts, so polling the plan would only duplicate those records",
    "http:GET /api/v1/auth/saml/{provider}/metadata": "unauthenticated documentation-class SP metadata: public entityID, ACS, certificate and request-signing policy; no principal, tenant data or state change, and the SAML ADR declares no read event",
    "http:GET /api/v1/auth/totp": "reports the caller's OWN TOTP factor state (whether a confirmed factor stands and whether an enrolment is mid-flight) and writes nothing; the factor's lifecycle is already recorded at enrol/remove time (auth.* factor events), so an event here would duplicate that once per poll, exactly like the passkey and identities reads",
    "http:GET /api/v1/auth/webauthn/credentials": "reports the caller's own enrolled passkeys and writes nothing; a self read whose subjects are already recorded at add/remove time (auth.passkey_added / auth.passkey_removed), so an event here would duplicate that once per poll, exactly like the identities read",
    "http:GET /api/v1/auth/whoami": "reports the session the caller already holds and writes nothing; the session's creation, method and assurance are already recorded by auth.session_created, so an event here would duplicate that record once per poll",
    "http:GET /api/v1/me/orgs": "reports the organisations the caller's OWN grants name and writes nothing; a self read whose subjects are already recorded when the grants are created or revoked, and whose result set can contain nothing the caller does not already hold authority in \u2014 an event here would record 'a principal looked at their own sidebar' once per page load, which no investigation could act on. Deliberately not the operator enumeration: org.list carries its own instance audit event and its own MFA-mandatory formula",
    "http:GET /api/v1/me/sessions": "reports the caller's OWN active sessions and writes nothing; a self read whose subjects are already recorded at issuance (auth.session_created, remote.workspace_session_issued) and at death (auth.logout, remote.workspace_session_revoked), so an event here would duplicate that once per poll \u2014 exactly like whoami and /api/v1/me/orgs. Deliberately NOT exempt: the DELETE beside it, which is an act and carries its events through wireEvents",
    "http:GET /api/v1/meta": "unauthenticated capability print: server version, API revision and which auth protocol flows this instance serves \u2014 no principal, no tenant data, no state change, and the ADR declares no event type for it (same class as cli:version, which it is the network form of)",
    "http:GET /healthz": "unauthenticated liveness probe: no principal, no tenant data, no state change; ADR names no health event type",
    "http:GET /metrics": "unauthenticated operational probe: the same persisted health fact as /healthz and /readyz, no principal, no tenant data, no state change, and no identity labels",
    "http:GET /readyz": "unauthenticated readiness probe: same contract as /healthz",
    "http:POST /api/v1/auth/webauthn/login/start": "opens a discoverable-credential ceremony and returns opaque options; no proof is validated, no credential or account is resolved, and no state a probe could distinguish is created \u2014 the login lands at login/finish, which carries the events",
    "http:POST /api/v1/auth/webauthn/reauth/start": "opens a ceremony bound to the operation unit and returns opaque options; validates no proof and changes no state \u2014 the window opens at reauth/finish, which carries the events",
    "http:POST /api/v1/auth/webauthn/step-up/start": "opens a ceremony scoped to the acting account's credentials and returns opaque options; validates no proof and changes no state \u2014 the elevation lands at step-up/finish, which carries the events"
  },
  "operations": {
    "definitions.plan.get": "reads an immutable definitions plan under definitions-edit authority and changes no state; plan creation and apply carry the auditable acts, so polling the plan would only duplicate those records",
    "scim-discovery.read": "SCIM discovery (#73 ADR \u00a710): the three static documents carry no tenant data, so no event type is declared. It cannot take `audited: none` under the default-deny permit rule (that rule admits only bare-`read`, non-mutating, tenant-class operations; this one authenticates a provisioning credential and records last-contact), so the ADR's demanded explicit annotation on the probe class is this pinned entry."
  }
}
