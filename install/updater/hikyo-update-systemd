#!/bin/sh
# Retired legacy deployment adapter. Refuse every phase, including rollback,
# before reading configuration, credentials, or invoking deployment commands.
printf '%s\n' 'Remote apply is disabled: the legacy updater cannot prove migration-safe rollback. Use the manual signed-bundle upgrade procedure at https://hikyo.app/docs/upgrades/.' >&2
exit 1
