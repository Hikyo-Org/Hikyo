# Private combined remediation candidate

This local integration combines the reviewed HTTP boundary, SCIM paging, human OIDC transport, client lifetime and workspace consent patches. All source patches were signed individually and reviewed before integration. Do not publish this branch or private evidence before coordinated release.

The only integration adjustment renews the sensitivity inventory hash for `routes/WorkspaceApprove.tsx` after an explicit parent review of the consent change. Its TanStack mutation stores only task/revision inputs and returns no approval result. The authorization code stays inside the active asynchronous operation and is sent only to the stored callback after component/session checks. TOTP input clears before dispatch and after settlement; the keyed reauthentication child retires on session change. Real AuthProvider tests cover deferred GET and POST responses on unmount or session replacement. This is an intentional reviewed inventory update, with no other hash refreshed.

Combined local validation passed at `addced855e8d7807589571ded973be499d057f1f`: generation, typechecks, production builds, 762 web tests, 20 TypeScript client tests, and 451 browser cases across desktop and mobile. All five intentional viewport skips passed in the other viewport; there were no failures or flakes. Both actual cross-tab login/logout cases and the recovery-code replacement regression passed.

Go core passed 4,128 named cases and the three non-isolation race shards passed 4,119 named cases at `202f278da428ca99ae53b2cffc63cb1369b16b83`. Source reconciliation confirms identical Go production and non-isolation test inputs at the final candidate. The only later Go changes were retention test fixtures, which passed separately. Final isolation coverage accounts for all 348 entrypoints: 345 direct passes and three dedicated fixture lanes, with no missing or unexpected cases. Fresh verified-TLS PostgreSQL, Forgejo and MCP rehearsals passed. The scoped external GitHub provider fixture remains an explicit external gate.

Evidence is retained under `/Users/developwent/.codex/artifacts/hikyo-1.0-20260905/private-remediation/`: `parent-review-status.json`, `source-reconciliation-202f-addced.json`, `final-browser-parent-verification.json`, `private-isolation-final-closure.json`, and the `combined-local-*` result/log directories. The implementation and integration reviews are CLEAN. This handoff update changes documentation only and preserves those tested source inputs.

Delivery uses private PR #2 in `Hikyo-Org/Hikyo-ghsa-2qjg-h73x-x9j4`. PR #1 is closed as superseded because GitHub advisory merging allows only one open PR targeting `main`. The combined candidate contains all five reviewed remediation patches; the transport-only branch must not be shipped alone.

Temporary private advisory forks disable hosted CI and status checks. Local validation is complete, but it does not satisfy the existing hosted-green merge condition. Obtain explicit approval to use the recorded local checks for this advisory merge, merge through the advisory workflow, and verify the resulting public commit and CI. `SECURITY.md` requires a patched release before advisory publication and a private CVE request before publication. No patched release exists yet; keep patched-version metadata unset and the advisory draft until the actual release is verified. Other advisories retain their separate release/publication gates.
