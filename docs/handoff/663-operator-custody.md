# #662/#663 operator installation custody

Implementation is in `internal/upgradegate/operator_custody.go`, `operator_custody_unix.go`, `operator_custody_unsupported.go`, `operator_rotation.go`, `gate.go`, `evidence.go`; `internal/store/upgrade/operator_rotation.go`, `candidate_keys.go`; `internal/app/operator.go`, `upgrade_gate.go`. Parent owns top-level command dispatch in `cmd/hikyo/main.go`.

- Production `Run` requires persistent state directory and configured public operator key. An installed public pin cannot silently change, even when no pending evidence exists. Pre-ledger export initializes only after authenticated actual-source discovery; an existing ledger refuses missing custody.
- Rotation authority: prior-key signature, or explicit local break-glass signed by the new key plus existing-root hierarchy proof. CLI: `hikyo upgrade operator rotate --statement FILE --signature FILE --new-public-key FILE`; break-glass additionally needs `--local-break-glass --root-key-file FILE`.
- External journal is fsynced inside DB writer/control-row exclusion after rechecking exact source and strongest epoch, before DB mutation, stores exact before/after state and public successor key, blocks admission until exact command resumes. DB transaction advances canonical strongest credential epoch, invalidates principal reconciliation, increments generation, randomizes recovery incarnation and invalidates pending evidence. New pin publishes only after DB commit.
- Historical restore keeps the current external pin and epoch floor. Prior-key rotation refuses DB epochs below that floor. Explicit local escrow-proved break-glass uses max(restored credential inventory, external floor), advances beyond both, and never revives the archive signer. The canonical authn sane-epoch limit of 2^32 remains enforced.
- Real SQLite and PostgreSQL tests: `GOMAXPROCS=2 go test -p 1 ./internal/upgradegate -run TestOperator -count=1 -json`, 17 cases pass, zero skipped, 8.319s. JSON evidence at `/tmp/hikyo-runtime-fence-evidence/operator-rotation.jsonl` and summary next to it. Parent owns final consolidated validation and CI.

Decision report: `docs/reports/663-operator-custody.html`. Production state directory is separate from the database and backup archive, euid-owned 0700 with owned single-link 0600 files; all replicas use one coherent shared custody location. Linux/macOS supported; unsupported platforms fail closed.

Windows client-only custody policy precedes this change: `internal/crypto/client_secure_windows.go:14-19`, `internal/disclose/file_windows.go:47`, `docs/reports/1.0/upgrade-attestations.html`. Windows archives still ship the client CLI; operator custody continues to fail closed outside supported Unix hosts. No Windows security-equivalence claim or server-support regression is introduced.

A crashed, uncommitted journal can be replaced only by a fresh valid prior-key or local-escrow request while complete DB authority still equals the original before-state. This handles ordinary credential epoch advancement after a crash without deleting custody. Committed journals accept only the exact originally signed request. Tests now inject failures in actual RotateOperator after journal fsync and DB commit, rather than manually synthesizing a journal.

Windows CLI crosscompile passed on final source: `GOMAXPROCS=2 GOOS=windows GOARCH=amd64 go build -p 1 -o /tmp/hikyo-runtime-fence-evidence/hikyo-windows.exe ./cmd/hikyo`. Test executable crosscompile also passed; no native Windows execution claimed.

R3 integration correction: direct public backup/runtime and data-recovery entrypoints previously bypassed external operator custody. `internal/app/backup_admission.go` now acquires `upgradegate.WithInstalledOperator` before datastore exclusion and checks actual source/epoch before runtime or recovery admission. Ordinary data restore imports hold the same scope. No root escrow is loaded. The public custody capability expires when its callback ends. Regression `internal/app/backup_operator_custody_test.go` proves production-config wrong signer and unfinished journal refusal before creating a database or writer lock, plus a root-free positive custody check. Focused ordinary export/data-restore/status + refusal suite passed 10 cases, zero skipped, real SQLite/PostgreSQL, 7.463s; evidence `/tmp/hikyo-runtime-fence-evidence/backup-custody.jsonl` and summary.
