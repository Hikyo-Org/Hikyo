// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type Options as Options2, type TDataShape, urlSearchParamsBodySerializer } from './client';
import { client } from './client.gen';
import type { AddAdapterTargetData, AddAdapterTargetErrors, AddAdapterTargetResponses, AddRemoteData, AddRemoteErrors, AddRemoteResponses, AddWorkspaceOriginData, AddWorkspaceOriginErrors, AddWorkspaceOriginResponses, AdoptAdapterTargetNamesData, AdoptAdapterTargetNamesErrors, AdoptAdapterTargetNamesResponses, ApplyDefinitionsPlanData, ApplyDefinitionsPlanErrors, ApplyDefinitionsPlanResponses, ApplyEnvTemplateData, ApplyEnvTemplateErrors, ApplyEnvTemplateResponses, ApplyInstanceTemplateData, ApplyInstanceTemplateErrors, ApplyInstanceTemplateResponses, ApplyOrgTemplateData, ApplyOrgTemplateErrors, ApplyOrgTemplateResponses, ApplyProjectTemplateData, ApplyProjectTemplateErrors, ApplyProjectTemplateResponses, ApproveCliReauthData, ApproveCliReauthErrors, ApproveCliReauthResponses, ApproveWorkspaceHandoffData, ApproveWorkspaceHandoffErrors, ApproveWorkspaceHandoffResponses, AuthMethodsData, AuthMethodsErrors, AuthMethodsResponses, BeginRecoveryData, BeginRecoveryErrors, BeginRecoveryResponses, CancelAdapterMoveData, CancelAdapterMoveErrors, CancelAdapterMoveResponses, CheckDefinitionsData, CheckDefinitionsErrors, CheckDefinitionsResponses, ClearValueData, ClearValueErrors, ClearValueResponses, CloneEnvironmentData, CloneEnvironmentErrors, CloneEnvironmentResponses, CompromiseRetireSamlSpKeyData, CompromiseRetireSamlSpKeyErrors, CompromiseRetireSamlSpKeyResponses, CopyValuesData, CopyValuesErrors, CopyValuesResponses, CreateAdapterData, CreateAdapterErrors, CreateAdapterResponses, CreateApprovalPolicyData, CreateApprovalPolicyErrors, CreateApprovalPolicyResponses, CreateDefinitionsPlanData, CreateDefinitionsPlanErrors, CreateDefinitionsPlanResponses, CreateDynamicProviderData, CreateDynamicProviderErrors, CreateDynamicProviderResponses, CreateEnvGrantData, CreateEnvGrantErrors, CreateEnvGrantResponses, CreateEnvironmentData, CreateEnvironmentErrors, CreateEnvironmentResponses, CreateFederatedBindingData, CreateFederatedBindingErrors, CreateFederatedBindingResponses, CreateFederationIssuerData, CreateFederationIssuerErrors, CreateFederationIssuerResponses, CreateFolderData, CreateFolderErrors, CreateFolderResponses, CreateInstanceGrantData, CreateInstanceGrantErrors, CreateInstanceGrantResponses, CreateKeyData, CreateKeyErrors, CreateKeyGroupData, CreateKeyGroupErrors, CreateKeyGroupResponses, CreateKeyResponses, CreateOrgData, CreateOrgErrors, CreateOrgGrantData, CreateOrgGrantErrors, CreateOrgGrantResponses, CreateOrgResponses, CreateProjectData, CreateProjectErrors, CreateProjectGrantData, CreateProjectGrantErrors, CreateProjectGrantResponses, CreateProjectResponses, CreateRevisionPinData, CreateRevisionPinErrors, CreateRevisionPinResponses, CreateScimBindingData, CreateScimBindingErrors, CreateScimBindingResponses, CreateScimMappingData, CreateScimMappingErrors, CreateScimMappingResponses, CreateServiceAccountData, CreateServiceAccountErrors, CreateServiceAccountResponses, DeclareValuesData, DeclareValuesErrors, DeclareValuesResponses, DeleteAdapterData, DeleteAdapterErrors, DeleteAdapterResponses, DeleteApprovalPolicyData, DeleteApprovalPolicyErrors, DeleteApprovalPolicyResponses, DeleteDynamicProviderData, DeleteDynamicProviderErrors, DeleteDynamicProviderResponses, DeleteEnvironmentData, DeleteEnvironmentErrors, DeleteEnvironmentResponses, DeleteFederationIssuerData, DeleteFederationIssuerErrors, DeleteFederationIssuerResponses, DeleteFolderData, DeleteFolderErrors, DeleteFolderResponses, DeleteKeyData, DeleteKeyErrors, DeleteKeyGroupData, DeleteKeyGroupErrors, DeleteKeyGroupResponses, DeleteKeyResponses, DeleteOidcProviderData, DeleteOidcProviderErrors, DeleteOidcProviderResponses, DeleteOrgData, DeleteOrgErrors, DeleteOrgResponses, DeleteProjectData, DeleteProjectErrors, DeleteProjectResponses, DeleteSamlProviderData, DeleteSamlProviderErrors, DeleteSamlProviderResponses, DeleteScimBindingData, DeleteScimBindingErrors, DeleteScimBindingResponses, DeleteScimMappingData, DeleteScimMappingErrors, DeleteScimMappingResponses, DeleteServiceAccountData, DeleteServiceAccountErrors, DeleteServiceAccountResponses, DiffValuesData, DiffValuesErrors, DiffValuesResponses, EnrolPasskeyFinishData, EnrolPasskeyFinishErrors, EnrolPasskeyFinishResponses, EnrolPasskeyStartData, EnrolPasskeyStartErrors, EnrolPasskeyStartResponses, EnrolTotpConfirmData, EnrolTotpConfirmErrors, EnrolTotpConfirmResponses, EnrolTotpStartData, EnrolTotpStartErrors, EnrolTotpStartResponses, EstablishCredentialData, EstablishCredentialErrors, EstablishCredentialResponses, ExportDefinitionsData, ExportDefinitionsErrors, ExportDefinitionsResponses, ExportEnvAuditData, ExportEnvAuditErrors, ExportEnvAuditResponses, ExportOrgAuditData, ExportOrgAuditErrors, ExportOrgAuditResponses, ExportProjectAuditData, ExportProjectAuditErrors, ExportProjectAuditResponses, ExportValuesData, ExportValuesErrors, ExportValuesResponses, FetchDeliveryData, FetchDeliveryErrors, FetchDeliveryResponses, GetApprovalCeremonyData, GetApprovalCeremonyErrors, GetApprovalCeremonyResponses, GetCredentialPolicyData, GetCredentialPolicyErrors, GetCredentialPolicyResponses, GetDefinitionsPlanData, GetDefinitionsPlanErrors, GetDefinitionsPlanResponses, GetDefinitionsSettingsData, GetDefinitionsSettingsErrors, GetDefinitionsSettingsResponses, GetEnvironmentData, GetEnvironmentErrors, GetEnvironmentResponses, GetEnvironmentSettingsData, GetEnvironmentSettingsErrors, GetEnvironmentSettingsResponses, GetEnvironmentSignalsData, GetEnvironmentSignalsErrors, GetEnvironmentSignalsResponses, GetFolderData, GetFolderErrors, GetFolderResponses, GetInstanceUpdateJobData, GetInstanceUpdateJobErrors, GetInstanceUpdateJobResponses, GetKeyData, GetKeyErrors, GetKeyGroupData, GetKeyGroupErrors, GetKeyGroupResponses, GetKeyResponses, GetMachineRevealData, GetMachineRevealErrors, GetMachineRevealResponses, GetMetaData, GetMetaErrors, GetMetaResponses, GetOidcProviderData, GetOidcProviderErrors, GetOidcProviderResponses, GetOrgData, GetOrgErrors, GetOrgResponses, GetOrgRetentionData, GetOrgRetentionErrors, GetOrgRetentionResponses, GetProjectData, GetProjectErrors, GetProjectResponses, GetProjectRetentionData, GetProjectRetentionErrors, GetProjectRetentionResponses, GetRetentionHealthData, GetRetentionHealthErrors, GetRetentionHealthResponses, GetRevealWindowData, GetRevealWindowErrors, GetRevealWindowResponses, GetRevisionData, GetRevisionErrors, GetRevisionResponses, GetSamlProviderData, GetSamlProviderErrors, GetSamlProviderResponses, GetScimBindingData, GetScimBindingErrors, GetScimBindingResponses, GetScimCredentialData, GetScimCredentialErrors, GetScimCredentialResponses, GetTotpStatusData, GetTotpStatusErrors, GetTotpStatusResponses, GetUpdateStatusData, GetUpdateStatusErrors, GetUpdateStatusResponses, GetValueData, GetValueErrors, GetValueResponses, ImportValuesData, ImportValuesErrors, ImportValuesResponses, InviteInstanceMemberData, InviteInstanceMemberErrors, InviteInstanceMemberResponses, InviteOrgMemberData, InviteOrgMemberErrors, InviteOrgMemberResponses, LinkIdentityData, LinkIdentityErrors, LinkIdentityResponses, ListAdaptersData, ListAdaptersErrors, ListAdaptersResponses, ListAdapterTargetsData, ListAdapterTargetsErrors, ListAdapterTargetsResponses, ListApprovalPoliciesData, ListApprovalPoliciesErrors, ListApprovalPoliciesResponses, ListApprovalRequestsData, ListApprovalRequestsErrors, ListApprovalRequestsResponses, ListDynamicProvidersData, ListDynamicProvidersErrors, ListDynamicProvidersResponses, ListEnvironmentsData, ListEnvironmentsErrors, ListEnvironmentsResponses, ListFederationIssuersData, ListFederationIssuersErrors, ListFederationIssuersResponses, ListFoldersData, ListFoldersErrors, ListFoldersResponses, ListIdentitiesData, ListIdentitiesErrors, ListIdentitiesResponses, ListInstanceConnectionsData, ListInstanceConnectionsErrors, ListInstanceConnectionsResponses, ListInstanceGrantsData, ListInstanceGrantsErrors, ListInstanceGrantsResponses, ListKeyGroupsData, ListKeyGroupsErrors, ListKeyGroupsResponses, ListKeysData, ListKeysErrors, ListKeysResponses, ListLeasesData, ListLeasesErrors, ListLeasesResponses, ListMachineCredentialsData, ListMachineCredentialsErrors, ListMachineCredentialsResponses, ListMyOrgsData, ListMyOrgsErrors, ListMyOrgsResponses, ListMySessionsData, ListMySessionsErrors, ListMySessionsResponses, ListOidcProvidersData, ListOidcProvidersErrors, ListOidcProvidersResponses, ListOrgGrantsData, ListOrgGrantsErrors, ListOrgGrantsResponses, ListOrgsData, ListOrgsErrors, ListOrgsResponses, ListPasskeysData, ListPasskeysErrors, ListPasskeysResponses, ListPendingDraftsData, ListPendingDraftsErrors, ListPendingDraftsResponses, ListProjectGrantsData, ListProjectGrantsErrors, ListProjectGrantsResponses, ListProjectsData, ListProjectsErrors, ListProjectsResponses, ListRemotesData, ListRemotesErrors, ListRemotesResponses, ListRevisionPinsData, ListRevisionPinsErrors, ListRevisionPinsResponses, ListRevisionsData, ListRevisionsErrors, ListRevisionsResponses, ListSamlProvidersData, ListSamlProvidersErrors, ListSamlProvidersResponses, ListSamlSpKeysData, ListSamlSpKeysErrors, ListSamlSpKeysResponses, ListScimBindingsData, ListScimBindingsErrors, ListScimBindingsResponses, ListScimCredentialsData, ListScimCredentialsErrors, ListScimCredentialsResponses, ListScimDirectoryGroupsData, ListScimDirectoryGroupsErrors, ListScimDirectoryGroupsResponses, ListScimDirectoryUsersData, ListScimDirectoryUsersErrors, ListScimDirectoryUsersResponses, ListScimMappingsData, ListScimMappingsErrors, ListScimMappingsResponses, ListServiceAccountsData, ListServiceAccountsErrors, ListServiceAccountsResponses, ListValueOccurrencesData, ListValueOccurrencesErrors, ListValueOccurrencesResponses, ListValuesData, ListValuesErrors, ListValuesResponses, ListWorkspaceOriginsData, ListWorkspaceOriginsErrors, ListWorkspaceOriginsResponses, LocalLoginData, LocalLoginErrors, LocalLoginResponses, LogoutData, LogoutErrors, LogoutResponses, MintInstanceConnectionData, MintInstanceConnectionErrors, MintInstanceConnectionResponses, MintLeaseData, MintLeaseErrors, MintLeaseResponses, MintMachineCredentialData, MintMachineCredentialErrors, MintMachineCredentialResponses, MintScimCredentialData, MintScimCredentialErrors, MintScimCredentialResponses, OidcCallbackData, OidcCallbackErrors, OidcCallbackResponses, OidcStartData, OidcStartErrors, OidcStartResponses, PasskeyLoginFinishData, PasskeyLoginFinishErrors, PasskeyLoginFinishResponses, PasskeyLoginStartData, PasskeyLoginStartErrors, PasskeyLoginStartResponses, PatchSamlProviderData, PatchSamlProviderErrors, PatchSamlProviderResponses, PauseAdapterTargetData, PauseAdapterTargetErrors, PauseAdapterTargetResponses, PlanAdapterTargetData, PlanAdapterTargetErrors, PlanAdapterTargetResponses, PublishPendingChangesData, PublishPendingChangesErrors, PublishPendingChangesResponses, PutOidcProviderData, PutOidcProviderErrors, PutOidcProviderResponses, PutSamlProviderData, PutSamlProviderErrors, PutSamlProviderResponses, QueryEnvAuditData, QueryEnvAuditErrors, QueryEnvAuditResponses, QueryOrgAuditData, QueryOrgAuditErrors, QueryOrgAuditResponses, QueryProjectAuditData, QueryProjectAuditErrors, QueryProjectAuditResponses, ReauthPasskeyFinishData, ReauthPasskeyFinishErrors, ReauthPasskeyFinishResponses, ReauthPasskeyStartData, ReauthPasskeyStartErrors, ReauthPasskeyStartResponses, ReauthTotpData, ReauthTotpErrors, ReauthTotpResponses, ReclassifyKeyData, ReclassifyKeyErrors, ReclassifyKeyResponses, ReconcileOfflineRecordsData, ReconcileOfflineRecordsErrors, ReconcileOfflineRecordsResponses, RedeemCliReauthData, RedeemCliReauthErrors, RedeemCliReauthResponses, RedeemWorkspaceHandoffData, RedeemWorkspaceHandoffErrors, RedeemWorkspaceHandoffResponses, ReencryptInstanceData, ReencryptInstanceErrors, ReencryptInstanceResponses, ReencryptProjectData, ReencryptProjectErrors, ReencryptProjectResponses, RefreshSamlProviderMetadataData, RefreshSamlProviderMetadataErrors, RefreshSamlProviderMetadataResponses, RegenerateRecoveryCodesData, RegenerateRecoveryCodesErrors, RegenerateRecoveryCodesResponses, ReleaseRevisionPinData, ReleaseRevisionPinErrors, ReleaseRevisionPinResponses, RemoveAdapterTargetData, RemoveAdapterTargetErrors, RemoveAdapterTargetResponses, RemovePasskeyData, RemovePasskeyErrors, RemovePasskeyResponses, RemoveRemoteData, RemoveRemoteErrors, RemoveRemoteResponses, RemoveTotpData, RemoveTotpErrors, RemoveTotpResponses, RemoveWorkspaceOriginData, RemoveWorkspaceOriginErrors, RemoveWorkspaceOriginResponses, RenameEnvironmentData, RenameEnvironmentErrors, RenameEnvironmentResponses, RenameFolderData, RenameFolderErrors, RenameFolderResponses, RenameKeyData, RenameKeyErrors, RenameKeyGroupData, RenameKeyGroupErrors, RenameKeyGroupResponses, RenameKeyResponses, RenameOrgData, RenameOrgErrors, RenameOrgResponses, RenameProjectData, RenameProjectErrors, RenameProjectResponses, RenameRemoteData, RenameRemoteErrors, RenameRemoteResponses, RenewLeaseData, RenewLeaseErrors, RenewLeaseResponses, ReorderEnvironmentsData, ReorderEnvironmentsErrors, ReorderEnvironmentsResponses, RequestInstanceUpdateData, RequestInstanceUpdateErrors, RequestInstanceUpdateResponses, ResetCredentialData, ResetCredentialErrors, ResetCredentialResponses, ResumeAdapterMoveData, ResumeAdapterMoveErrors, ResumeAdapterMoveResponses, ResumeAdapterTargetData, ResumeAdapterTargetErrors, ResumeAdapterTargetResponses, RetireSamlSpKeyData, RetireSamlSpKeyErrors, RetireSamlSpKeyResponses, RevealValueData, RevealValueDiffData, RevealValueDiffErrors, RevealValueDiffResponses, RevealValueErrors, RevealValueResponses, RevealValuesData, RevealValuesErrors, RevealValuesResponses, RevokeAdapterCredentialData, RevokeAdapterCredentialErrors, RevokeAdapterCredentialResponses, RevokeDynamicProviderCredentialData, RevokeDynamicProviderCredentialErrors, RevokeDynamicProviderCredentialResponses, RevokeEnvGrantData, RevokeEnvGrantErrors, RevokeEnvGrantResponses, RevokeInstanceConnectionData, RevokeInstanceConnectionErrors, RevokeInstanceConnectionResponses, RevokeInstanceGrantData, RevokeInstanceGrantErrors, RevokeInstanceGrantResponses, RevokeLeaseData, RevokeLeaseErrors, RevokeLeaseResponses, RevokeMachineCredentialData, RevokeMachineCredentialErrors, RevokeMachineCredentialResponses, RevokeMySessionData, RevokeMySessionErrors, RevokeMySessionResponses, RevokeOrgGrantData, RevokeOrgGrantErrors, RevokeOrgGrantResponses, RevokeProjectGrantData, RevokeProjectGrantErrors, RevokeProjectGrantResponses, RevokeScimCredentialData, RevokeScimCredentialErrors, RevokeScimCredentialResponses, RollbackRevisionData, RollbackRevisionErrors, RollbackRevisionResponses, RotateDekData, RotateDekErrors, RotateDekResponses, RotateMasterKeyData, RotateMasterKeyErrors, RotateMasterKeyResponses, RotateRootKeyData, RotateRootKeyErrors, RotateRootKeyResponses, RotateSamlSpKeyData, RotateSamlSpKeyErrors, RotateSamlSpKeyResponses, RotateScanningKeyData, RotateScanningKeyErrors, RotateScanningKeyResponses, RotateTokenKeyData, RotateTokenKeyErrors, RotateTokenKeyResponses, SamlAcsData, SamlAcsErrors, SamlAcsResponses, SamlMetadataData, SamlMetadataErrors, SamlMetadataResponses, SamlStartData, SamlStartErrors, SamlStartResponses, ScimBulkData, ScimBulkErrors, ScimCreateGroupData, ScimCreateGroupErrors, ScimCreateGroupResponses, ScimCreateUserData, ScimCreateUserErrors, ScimCreateUserResponses, ScimDeleteGroupData, ScimDeleteGroupErrors, ScimDeleteGroupResponses, ScimDeleteUserData, ScimDeleteUserErrors, ScimDeleteUserResponses, ScimGetGroupData, ScimGetGroupErrors, ScimGetGroupResponses, ScimGetUserData, ScimGetUserErrors, ScimGetUserResponses, ScimListGroupsData, ScimListGroupsErrors, ScimListGroupsResponses, ScimListUsersData, ScimListUsersErrors, ScimListUsersResponses, ScimMeData, ScimMeErrors, ScimPatchGroupData, ScimPatchGroupErrors, ScimPatchGroupResponses, ScimPatchUserData, ScimPatchUserErrors, ScimPatchUserResponses, ScimReplaceGroupData, ScimReplaceGroupErrors, ScimReplaceGroupResponses, ScimReplaceUserData, ScimReplaceUserErrors, ScimReplaceUserResponses, ScimResourceTypesData, ScimResourceTypesErrors, ScimResourceTypesResponses, ScimSchemasData, ScimSchemasErrors, ScimSchemasResponses, ScimSearchGroupsData, ScimSearchGroupsErrors, ScimSearchUsersData, ScimSearchUsersErrors, ScimServiceProviderConfigData, ScimServiceProviderConfigErrors, ScimServiceProviderConfigResponses, ServeDirectoryData, ServeDirectoryErrors, ServeDirectoryResponses, SetAdapterCredentialData, SetAdapterCredentialErrors, SetAdapterCredentialResponses, SetCredentialPolicyData, SetCredentialPolicyErrors, SetCredentialPolicyResponses, SetDefinitionsSettingsData, SetDefinitionsSettingsErrors, SetDefinitionsSettingsResponses, SetDynamicProviderCredentialData, SetDynamicProviderCredentialErrors, SetDynamicProviderCredentialResponses, SetEnvironmentSettingsData, SetEnvironmentSettingsErrors, SetEnvironmentSettingsResponses, SetKeyGroupData, SetKeyGroupErrors, SetKeyGroupResponses, SetMachineRevealData, SetMachineRevealErrors, SetMachineRevealResponses, SetOrgRetentionData, SetOrgRetentionErrors, SetOrgRetentionResponses, SetProjectRetentionData, SetProjectRetentionErrors, SetProjectRetentionResponses, SettleLeaseData, SettleLeaseErrors, SettleLeaseResponses, SetValueData, SetValueErrors, SetValueResponses, ShowAdapterData, ShowAdapterErrors, ShowAdapterMoveData, ShowAdapterMoveErrors, ShowAdapterMoveResponses, ShowAdapterResponses, ShowAdapterTargetData, ShowAdapterTargetErrors, ShowAdapterTargetResponses, ShowCliReauthTransactionData, ShowCliReauthTransactionErrors, ShowCliReauthTransactionResponses, ShowDynamicProviderData, ShowDynamicProviderErrors, ShowDynamicProviderResponses, ShowInstanceConnectionData, ShowInstanceConnectionErrors, ShowInstanceConnectionResponses, ShowLeaseData, ShowLeaseErrors, ShowLeaseResponses, ShowRemoteData, ShowRemoteErrors, ShowRemoteResponses, ShowWorkspaceHandoffData, ShowWorkspaceHandoffErrors, ShowWorkspaceHandoffResponses, StartCliReauthData, StartCliReauthErrors, StartCliReauthResponses, StartWorkspaceHandoffData, StartWorkspaceHandoffErrors, StartWorkspaceHandoffResponses, StepUpPasskeyFinishData, StepUpPasskeyFinishErrors, StepUpPasskeyFinishResponses, StepUpPasskeyStartData, StepUpPasskeyStartErrors, StepUpPasskeyStartResponses, StepUpTotpData, StepUpTotpErrors, StepUpTotpResponses, SyncAdapterTargetData, SyncAdapterTargetErrors, SyncAdapterTargetResponses, TestAdapterTargetData, TestAdapterTargetErrors, TestAdapterTargetResponses, UnlinkIdentityData, UnlinkIdentityErrors, UnlinkIdentityResponses, UpdateAdapterOriginData, UpdateAdapterOriginErrors, UpdateAdapterOriginResponses, UpdateAdapterTargetData, UpdateAdapterTargetErrors, UpdateAdapterTargetResponses, UpdateApprovalPolicyData, UpdateApprovalPolicyErrors, UpdateApprovalPolicyResponses, UpdateFederationIssuerData, UpdateFederationIssuerErrors, UpdateFederationIssuerResponses, UpdateKeyDeclarationData, UpdateKeyDeclarationErrors, UpdateKeyDeclarationResponses, UpdateKeyMetadataData, UpdateKeyMetadataErrors, UpdateKeyMetadataResponses, UpdateScimMappingData, UpdateScimMappingErrors, UpdateScimMappingResponses, VoteApprovalRequestData, VoteApprovalRequestErrors, VoteApprovalRequestResponses, WatchProjectEventsData, WatchProjectEventsErrors, WatchProjectEventsResponse, WatchProjectEventsResponses, WhoamiData, WhoamiErrors, WhoamiResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Instance discovery, unauthenticated.
 *
 * The response is an exact closed allowlist: server version, API
 * revision, and which authentication protocol flows this instance
 * serves. Nothing tenant- or configuration-shaped — capability
 * discovery beyond protocol selection happens after authentication.
 *
 * Sits under the instance-wide pre-auth admission limits like every
 * other pre-auth path.
 *
 */
export const getMeta = <ThrowOnError extends boolean = false>(options?: Options<GetMetaData, ThrowOnError>) => (options?.client ?? client).get<GetMetaResponses, GetMetaErrors, ThrowOnError>({ url: '/api/v1/meta', ...options });

/**
 * Consume a credential-establishment authority and set an initial credential.
 *
 * The named — and only — exception to "a new credential may never
 * authorize its own enrolment" (human-auth ADR § Credential-establishment
 * authority). The authority is target-bound, purpose-bound, single-use,
 * expiring, and issued only by the bootstrap path, `credential-reset`,
 * a member invitation, local break-glass, or recovery.
 *
 * Consuming it establishes exactly one initial credential, atomically,
 * and nothing more: **no session is created, no assurance is carried,
 * no reauthentication window opens.** The holder authenticates
 * afterwards with the credential they just set, like anyone else.
 *
 * Every refusal — unknown authority, expired, already consumed, wrong
 * epoch, weak password — answers the same `401` so presentation reveals
 * nothing about which authorities exist.
 *
 */
export const establishCredential = <ThrowOnError extends boolean = false>(options: Options<EstablishCredentialData, ThrowOnError>) => (options.client ?? client).post<EstablishCredentialResponses, EstablishCredentialErrors, ThrowOnError>({
    url: '/api/v1/auth/credential/establish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Local password login; mints a CLI or browser session artifact.
 *
 * The local floor (human-auth ADR § Lockout and the local floor), which
 * an installation may never remove. Verifies an Argon2id password
 * verifier inside the instance-wide admission budget; an unknown account
 * traverses a bounded dummy-verifier path so timing is comparable and no
 * pre-authentication path distinguishes an existing account from a
 * missing one.
 *
 * Mints the artifact the request asks for. Both are distinct types with
 * their own storage, lifetime, audit identity and revocation surface,
 * and neither value is ever re-retrievable:
 *
 * - **CLI session** (default): a replayable bearer credential returned
 * in the response body, presented on `Authorization: Bearer`.
 * - **Browser session**: delivered ONLY on the `__Host-hikyo` HttpOnly
 * cookie, never in the body, so injected same-origin script cannot
 * read it. Its synchronizer token rides the `__Host-hikyo-csrf`
 * companion cookie and must be echoed on the `X-Hikyo-CSRF` header for
 * every state-changing cookie-authenticated request.
 *
 * See `/api/v1/meta` for which protocol flows an instance actually
 * offers.
 *
 */
export const localLogin = <ThrowOnError extends boolean = false>(options: Options<LocalLoginData, ThrowOnError>) => (options.client ?? client).post<LocalLoginResponses, LocalLoginErrors, ThrowOnError>({
    url: '/api/v1/auth/local/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke the presented session.
 *
 * Deletes the presented session row. Idempotent in effect: a session
 * that no longer resolves answers `401` like any other unauthenticated
 * request, because there is nothing to revoke and nothing to report.
 *
 */
export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>) => (options?.client ?? client).post<LogoutResponses, LogoutErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/logout',
    ...options
});

/**
 * Describe the presented session.
 *
 * Reports the acting principal and how **this session** authenticated —
 * the assurance record the chokepoint consults, never what the account
 * could have presented (human-auth ADR § Assurance).
 *
 */
export const whoami = <ThrowOnError extends boolean = false>(options?: Options<WhoamiData, ThrowOnError>) => (options?.client ?? client).get<WhoamiResponses, WhoamiErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/whoami',
    ...options
});

/**
 * Begin TOTP enrolment; returns the otpauth URI once.
 *
 * An account-security proof up front (the pre-existing password, since no
 * possession factor stands yet — human-auth ADR § Account-security
 * mutations). Stages a sealed seed as a pending, inert enrolment and
 * returns the `otpauth://` URI exactly once. It confirms nothing and
 * mutates no session: `enrol/confirm` completes the enrolment. A confirmed
 * factor already standing is refused (`400`).
 *
 */
export const enrolTotpStart = <ThrowOnError extends boolean = false>(options: Options<EnrolTotpStartData, ThrowOnError>) => (options.client ?? client).post<EnrolTotpStartResponses, EnrolTotpStartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/totp/enrol/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Confirm TOTP enrolment with a code; reissues the session.
 *
 * Consumes a valid code against the pending seed (single-use) and
 * completes the enrolment as an account-security mutation: the generation
 * advances, every session is deleted, and the acting session is reissued
 * carrying ONLY the proof class — NOT `totp`. The user steps up separately
 * to present the factor they just enrolled. The reissued token is returned
 * and MUST replace the old one.
 *
 */
export const enrolTotpConfirm = <ThrowOnError extends boolean = false>(options: Options<EnrolTotpConfirmData, ThrowOnError>) => (options.client ?? client).post<EnrolTotpConfirmResponses, EnrolTotpConfirmErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/totp/enrol/confirm',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Elevate the acting session by presenting a TOTP code.
 *
 * A valid code consumes its step (single-use) and appends `totp` to the
 * acting session's factor set, rotating its token — not an
 * account-security mutation, so no generation advance and no session
 * deletion, and the original authenticated_at is preserved (human-auth ADR
 * § Assurance). The rotated token is returned and MUST replace the old one.
 *
 */
export const stepUpTotp = <ThrowOnError extends boolean = false>(options: Options<StepUpTotpData, ThrowOnError>) => (options.client ?? client).post<StepUpTotpResponses, StepUpTotpErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/totp/step-up',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Open a disclosure reauthentication window with a TOTP code.
 *
 * The possession-factor half of the reveal ceremony (#58,
 * permission-model ADR § The reveal guard). It is DISCLOSURE
 * reauthentication, not account step-up: it opens a sliding window over
 * one environment and changes nothing about the account.
 *
 * The environment is RESOLVED AND AUTHORIZED under `read(E)` before its
 * reauthentication policy is looked at. An environment the caller cannot
 * reach answers exactly as a nonexistent one does — the uniform `401` this
 * route gives every other refusal — so the policy-shaped `409` below is
 * never an oracle for which environment ids exist or which are protected.
 *
 * TOTP cannot bind its challenge to an enumerated unit, so it cannot
 * honour a per-disclosure gate. Where the environment's effective window
 * is `0` — which every protected environment is capped at — this route
 * REFUSES with `409`, naming the passkey ceremony as the remedy, and
 * `/auth/webauthn/reauth*` is the only path that opens a window there.
 *
 * The reauth rotates the acting session; a browser session's rotated
 * token is delivered on the `__Host-hikyo` cookie.
 *
 */
export const reauthTotp = <ThrowOnError extends boolean = false>(options: Options<ReauthTotpData, ThrowOnError>) => (options.client ?? client).post<ReauthTotpResponses, ReauthTotpErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/reauth/totp',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Bind an adapter reauthentication handoff to the acting CLI session and PKCE challenge.
 */
export const startCliReauth = <ThrowOnError extends boolean = false>(options: Options<StartCliReauthData, ThrowOnError>) => (options.client ?? client).post<StartCliReauthResponses, StartCliReauthErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/cli-reauth/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Load display-only policy and exact callback metadata for a live CLI handoff.
 */
export const showCliReauthTransaction = <ThrowOnError extends boolean = false>(options: Options<ShowCliReauthTransactionData, ThrowOnError>) => (options.client ?? client).get<ShowCliReauthTransactionResponses, ShowCliReauthTransactionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/cli-reauth/transactions/{state}',
    ...options
});

/**
 * Approve a CLI adapter ceremony with the browser session's exact bound windows.
 */
export const approveCliReauth = <ThrowOnError extends boolean = false>(options: Options<ApproveCliReauthData, ThrowOnError>) => (options.client ?? client).post<ApproveCliReauthResponses, ApproveCliReauthErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/cli-reauth/approve',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Atomically exchange code and PKCE verifier for the rotated CLI bearer.
 */
export const redeemCliReauth = <ThrowOnError extends boolean = false>(options: Options<RedeemCliReauthData, ThrowOnError>) => (options.client ?? client).post<RedeemCliReauthResponses, RedeemCliReauthErrors, ThrowOnError>({
    url: '/api/v1/auth/cli-reauth/redeem',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove the confirmed TOTP factor.
 *
 * An account-security mutation authorized by the password: the factor
 * being removed is excluded from the proof set, so a stolen phone alone
 * cannot drop the very factor it is (human-auth ADR § Account-security
 * mutations, finding B7). Reissues the acting session carrying only the
 * password class; the reissued token is returned.
 *
 */
export const removeTotp = <ThrowOnError extends boolean = false>(options: Options<RemoveTotpData, ThrowOnError>) => (options.client ?? client).delete<RemoveTotpResponses, RemoveTotpErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/totp',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Report whether the caller has a TOTP factor.
 *
 * Reads the caller's OWN authenticator state: whether a confirmed factor
 * stands, and whether an enrolment is mid-flight (a staged seed awaiting
 * its first confirming code). It discloses nothing a caller could not
 * already learn by attempting an enrolment — a second start is refused by
 * name — so the account surface can state the fact rather than disclaim
 * knowledge of it.
 *
 */
export const getTotpStatus = <ThrowOnError extends boolean = false>(options?: Options<GetTotpStatusData, ThrowOnError>) => (options?.client ?? client).get<GetTotpStatusResponses, GetTotpStatusErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/totp',
    ...options
});

/**
 * Replace the recovery-code batch; returns the codes once.
 *
 * An account-security mutation proved by the confirmed TOTP code where one
 * stands, else the password (recovery codes never authorize their own
 * regeneration). Replaces the batch atomically and returns the plaintext
 * codes exactly once. Reissues the acting session; the reissued token is
 * returned and MUST replace the old one.
 *
 */
export const regenerateRecoveryCodes = <ThrowOnError extends boolean = false>(options: Options<RegenerateRecoveryCodesData, ThrowOnError>) => (options.client ?? client).post<RegenerateRecoveryCodesResponses, RegenerateRecoveryCodesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/recovery-codes/regenerate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Consume a recovery code for a credential-establishment authority.
 *
 * The pre-auth break-in-glass path. Consumes a single-use recovery code
 * and mints a credential-establishment authority that may only ever
 * establish a password (human-auth ADR § Recovery). It creates **no
 * session, no assurance, no reauthentication window** — the holder
 * establishes a new password with the authority and then logs in.
 *
 * Rides the instance-wide admission budget; an unknown username, an absent
 * batch, a stale epoch and a non-matching code all answer the same `401`.
 *
 */
export const beginRecovery = <ThrowOnError extends boolean = false>(options: Options<BeginRecoveryData, ThrowOnError>) => (options.client ?? client).post<BeginRecoveryResponses, BeginRecoveryErrors, ThrowOnError>({
    url: '/api/v1/auth/recovery/begin',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List organisations.
 *
 * An instance-scoped operator read of cross-tenant metadata, and
 * therefore itself audited — the audit model's default-deny rule refuses
 * `audited: none` to instance-class operations. The event commits with
 * the read.
 *
 */
export const listOrgs = <ThrowOnError extends boolean = false>(options?: Options<ListOrgsData, ThrowOnError>) => (options?.client ?? client).get<ListOrgsResponses, ListOrgsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs',
    ...options
});

/**
 * Create an organisation.
 *
 * Authorization is evaluated at the chokepoint inside the request's own
 * transaction. The organisation, `org.created` event, and creator's org
 * `admin` template grants commit atomically. The privilege increase ends
 * the creator's sessions; the new membership is usable after login.
 *
 * Not idempotent: each call creates a distinct organisation. The name must
 * be unused among live organisations; a collision is a `conflict`.
 *
 */
export const createOrg = <ThrowOnError extends boolean = false>(options: Options<CreateOrgData, ThrowOnError>) => (options.client ?? client).post<CreateOrgResponses, CreateOrgErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an organisation.
 *
 * Never cascades. An organisation that still holds projects, or grants
 * pointing into it, is refused with `conflict` — emptying a tenant is
 * explicit, separately audited work and not a side effect of one call.
 *
 */
export const deleteOrg = <ThrowOnError extends boolean = false>(options: Options<DeleteOrgData, ThrowOnError>) => (options.client ?? client).delete<DeleteOrgResponses, DeleteOrgErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}',
    ...options
});

/**
 * Read one organisation.
 *
 * Tenant-scoped at org depth, unlike the create and list operations
 * above: an organisation the caller may not reach answers exactly like
 * one that does not exist, which is what "unauthorized access at each
 * level returns the uniform nonexistent shape" requires of the top level
 * too. There is therefore no `403` on this route — a refusal is a `404`.
 *
 */
export const getOrg = <ThrowOnError extends boolean = false>(options: Options<GetOrgData, ThrowOnError>) => (options.client ?? client).get<GetOrgResponses, GetOrgErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}',
    ...options
});

/**
 * Rename an organisation.
 *
 * Requires manage-members at the addressed organisation. Organisation
 * administrators can rename their own organisation; instance-level
 * manage-members grants satisfy this by inheritance. A bare
 * instance-config grant does not authorize renaming. Refusal is the
 * uniform 404. Deleting remains an instance-config operation.
 *
 */
export const renameOrg = <ThrowOnError extends boolean = false>(options: Options<RenameOrgData, ThrowOnError>) => (options.client ?? client).patch<RenameOrgResponses, RenameOrgErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the organisation's projects.
 *
 * A proof-scoped read at org depth. Unpaged: the hierarchy is bounded by
 * the ops spec's own caps (50 environments per project) and an
 * organisation's project list is administrative, not a data feed.
 *
 */
export const listProjects = <ThrowOnError extends boolean = false>(options: Options<ListProjectsData, ThrowOnError>) => (options.client ?? client).get<ListProjectsResponses, ListProjectsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects',
    ...options
});

/**
 * Create a project.
 *
 * Not idempotent: each call creates a distinct project. The name must be
 * unused among the organisation's live projects; a collision is a
 * `conflict`, decided after authorization so it discloses nothing a
 * caller could not already read.
 *
 */
export const createProject = <ThrowOnError extends boolean = false>(options: Options<CreateProjectData, ThrowOnError>) => (options.client ?? client).post<CreateProjectResponses, CreateProjectErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read the organisation retention cap.
 *
 * The default is keep-if-either: age at most 90 days OR among the last
 * 10 revisions of an environment. Unlimited is explicit org state, never
 * a missing-value default.
 *
 */
export const getOrgRetention = <ThrowOnError extends boolean = false>(options: Options<GetOrgRetentionData, ThrowOnError>) => (options.client ?? client).get<GetOrgRetentionResponses, GetOrgRetentionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/retention',
    ...options
});

/**
 * Set the organisation retention cap.
 */
export const setOrgRetention = <ThrowOnError extends boolean = false>(options: Options<SetOrgRetentionData, ThrowOnError>) => (options.client ?? client).put<SetOrgRetentionResponses, SetOrgRetentionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/retention',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a project.
 *
 * Never cascades. A project that still holds environments or folders is
 * refused with `conflict`; emptying it is the caller's explicit,
 * separately audited work.
 *
 */
export const deleteProject = <ThrowOnError extends boolean = false>(options: Options<DeleteProjectData, ThrowOnError>) => (options.client ?? client).delete<DeleteProjectResponses, DeleteProjectErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}',
    ...options
});

/**
 * Read one project.
 */
export const getProject = <ThrowOnError extends boolean = false>(options: Options<GetProjectData, ThrowOnError>) => (options.client ?? client).get<GetProjectResponses, GetProjectErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}',
    ...options
});

/**
 * Rename a project.
 *
 * Changes the mutable name only. Identity is the immutable id, so a
 * rename never breaks a reference. Idempotent: renaming to the current
 * name succeeds and changes nothing.
 *
 */
export const renameProject = <ThrowOnError extends boolean = false>(options: Options<RenameProjectData, ThrowOnError>) => (options.client ?? client).patch<RenameProjectResponses, RenameProjectErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the project's environments, in display order.
 *
 * Ordered by `display_order`, ties broken by name so the sequence is
 * deterministic before any reorder has happened.
 *
 */
export const listEnvironments = <ThrowOnError extends boolean = false>(options: Options<ListEnvironmentsData, ThrowOnError>) => (options.client ?? client).get<ListEnvironmentsResponses, ListEnvironmentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments',
    ...options
});

/**
 * Create an environment.
 *
 * Environments are user-defined per project and appended at the end of
 * the display order; `PUT .../environments/order` moves them. There is no
 * `base` pointer and no defaults layer to inherit from — every value is
 * explicit per environment (flat-model ADR).
 *
 * A project holds at most 50 environments; the 51st is refused by name
 * with `limit_exceeded`, decided against a count read inside the creating
 * transaction.
 *
 */
export const createEnvironment = <ThrowOnError extends boolean = false>(options: Options<CreateEnvironmentData, ThrowOnError>) => (options.client ?? client).post<CreateEnvironmentResponses, CreateEnvironmentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create an environment holding a copy of another one's values.
 *
 * Clone-at-creation is ONE act: the environment row, every copied value
 * and the audit records for both commit together or not at all - the
 * atomicity rule admits no partially-valid creation.
 *
 * The copy is preflighted. `config` values copy freely (freely meaning
 * without the reveal gate, not without authorization); `secret` values
 * copy only where the source-material gate passes, and a caller who
 * cannot read them is NOT refused the creation - what could not be
 * copied comes back enumerated by name. The one exception is the abort:
 * a `secret` that would be ABSENT in the new environment - the gate
 * blocked it, or the source never held it - and is `required_in` it
 * under a `mode: all` rule ABORTS the creation, naming the keys, because
 * an environment born invalid is exactly what the atomicity rule
 * forbids.
 *
 * It is a separate route rather than a flag on `createEnvironment`
 * because its RESPONSE is different: a clone has to report what it could
 * not take.
 *
 */
export const cloneEnvironment = <ThrowOnError extends boolean = false>(options: Options<CloneEnvironmentData, ThrowOnError>) => (options.client ?? client).post<CloneEnvironmentResponses, CloneEnvironmentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/clone',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Rewrite the project's environment display order.
 *
 * Takes the WHOLE ordered set, not one position: a single transaction
 * then cannot leave two environments sharing a position or a gap in the
 * sequence, and a concurrent reorder serializes behind it.
 *
 * The list must name exactly the project's environments, once each.
 * Anything else — a short list, a duplicate, an id from elsewhere — is one
 * `bad_request` with one fixed message, so a foreign id discloses nothing.
 *
 * Idempotent: applying the same order twice changes nothing.
 *
 */
export const reorderEnvironments = <ThrowOnError extends boolean = false>(options: Options<ReorderEnvironmentsData, ThrowOnError>) => (options.client ?? client).put<ReorderEnvironmentsResponses, ReorderEnvironmentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/order',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an environment.
 *
 * The remaining environments keep their positions: display order is a
 * property of each row, not an invariant over the set, so a gap is not a
 * defect and closing it would be an unrequested write.
 *
 */
export const deleteEnvironment = <ThrowOnError extends boolean = false>(options: Options<DeleteEnvironmentData, ThrowOnError>) => (options.client ?? client).delete<DeleteEnvironmentResponses, DeleteEnvironmentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}',
    ...options
});

/**
 * Read one environment.
 */
export const getEnvironment = <ThrowOnError extends boolean = false>(options: Options<GetEnvironmentData, ThrowOnError>) => (options.client ?? client).get<GetEnvironmentResponses, GetEnvironmentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}',
    ...options
});

/**
 * Rename an environment.
 *
 * Environment topology, so it carries `definitions-edit` rather than
 * `edit`: renaming is a change to the project's shape, not to a value.
 *
 */
export const renameEnvironment = <ThrowOnError extends boolean = false>(options: Options<RenameEnvironmentData, ThrowOnError>) => (options.client ?? client).patch<RenameEnvironmentResponses, RenameEnvironmentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read the project's effective retention policy.
 */
export const getProjectRetention = <ThrowOnError extends boolean = false>(options: Options<GetProjectRetentionData, ThrowOnError>) => (options.client ?? client).get<GetProjectRetentionResponses, GetProjectRetentionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/retention',
    ...options
});

/**
 * Set or clear the project's bounded retention override.
 *
 * inherited=true clears the override. A stored override must be at or
 * below the organisation cap in both age and revision count; exceeding
 * either is a named bad-request refusal.
 *
 */
export const setProjectRetention = <ThrowOnError extends boolean = false>(options: Options<SetProjectRetentionData, ThrowOnError>) => (options.client ?? client).put<SetProjectRetentionResponses, SetProjectRetentionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/retention',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the project's folders.
 */
export const listFolders = <ThrowOnError extends boolean = false>(options: Options<ListFoldersData, ThrowOnError>) => (options.client ?? client).get<ListFoldersResponses, ListFoldersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/folders',
    ...options
});

/**
 * Create a folder.
 *
 * A folder is organizational only in v1: namespace and display grouping.
 * No grant is ever scoped to one and no value ever attaches to one.
 *
 */
export const createFolder = <ThrowOnError extends boolean = false>(options: Options<CreateFolderData, ThrowOnError>) => (options.client ?? client).post<CreateFolderResponses, CreateFolderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/folders',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a folder.
 */
export const deleteFolder = <ThrowOnError extends boolean = false>(options: Options<DeleteFolderData, ThrowOnError>) => (options.client ?? client).delete<DeleteFolderResponses, DeleteFolderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/folders/{folder}',
    ...options
});

/**
 * Read one folder.
 */
export const getFolder = <ThrowOnError extends boolean = false>(options: Options<GetFolderData, ThrowOnError>) => (options.client ?? client).get<GetFolderResponses, GetFolderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/folders/{folder}',
    ...options
});

/**
 * Move a folder to a new path.
 *
 * Renames exactly the folder named. A v1 folder is a flat namespace
 * label, not a tree node with children to carry along, so there is no
 * cascade.
 *
 */
export const renameFolder = <ThrowOnError extends boolean = false>(options: Options<RenameFolderData, ThrowOnError>) => (options.client ?? client).patch<RenameFolderResponses, RenameFolderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/folders/{folder}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke one capability at instance scope.
 *
 * Releases the origins this surface owns and deletes the grant row when
 * the last one is gone, advancing the target's session generation and
 * deleting their sessions in the same transaction. Refused when it would
 * remove the last instance `manage-members` holder.
 *
 */
export const revokeInstanceGrant = <ThrowOnError extends boolean = false>(options: Options<RevokeInstanceGrantData, ThrowOnError>) => (options.client ?? client).delete<RevokeInstanceGrantResponses, RevokeInstanceGrantErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/grants',
    ...options
});

/**
 * List the instance-scope membership surface.
 *
 * One line per capability per principal, with the origins holding it.
 * Grants inside an org are NOT listed here even though instance-scope
 * grants reach into every org: membership of a scope is what was granted
 * AT it.
 *
 */
export const listInstanceGrants = <ThrowOnError extends boolean = false>(options?: Options<ListInstanceGrantsData, ThrowOnError>) => (options?.client ?? client).get<ListInstanceGrantsResponses, ListInstanceGrantsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/grants',
    ...options
});

/**
 * Grant one capability at instance scope.
 */
export const createInstanceGrant = <ThrowOnError extends boolean = false>(options: Options<CreateInstanceGrantData, ThrowOnError>) => (options.client ?? client).post<CreateInstanceGrantResponses, CreateInstanceGrantErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/grants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Apply a role template at instance scope.
 *
 * The template expands AT GRANT TIME into individual capability grants,
 * each separately visible and separately revocable. Nothing stores the
 * template name.
 *
 */
export const applyInstanceTemplate = <ThrowOnError extends boolean = false>(options: Options<ApplyInstanceTemplateData, ThrowOnError>) => (options.client ?? client).post<ApplyInstanceTemplateResponses, ApplyInstanceTemplateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/grants/template',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Invite a human at instance scope with a local credential.
 *
 * The instance-scope form of `inviteOrgMember`: the same one-transaction
 * account creation and authority mint, authorized by `manage-members`
 * held at instance scope. The optional template is expanded at instance
 * scope (`operator`), so this is how a second instance operator comes to
 * exist without host access. Without a template the account can sign in
 * and see nothing until someone grants it something.
 *
 * A username already in use is a `conflict`.
 *
 */
export const inviteInstanceMember = <ThrowOnError extends boolean = false>(options: Options<InviteInstanceMemberData, ThrowOnError>) => (options.client ?? client).post<InviteInstanceMemberResponses, InviteInstanceMemberErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/invitations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke one capability at organisation scope.
 *
 * Refused when it would remove the last org `manage-members` holder: an
 * unadministrable organisation is a support incident with no in-product
 * recovery.
 *
 */
export const revokeOrgGrant = <ThrowOnError extends boolean = false>(options: Options<RevokeOrgGrantData, ThrowOnError>) => (options.client ?? client).delete<RevokeOrgGrantResponses, RevokeOrgGrantErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/grants',
    ...options
});

/**
 * List the organisation's membership surface.
 *
 * Every grant line scoped inside the org — org, project and environment
 * scoped alike — with its origin chips. Instance-scope grants that reach
 * the org by inheritance are deliberately absent: listing them here would
 * invite revoking an instance operator from a page with no authority over
 * one.
 *
 */
export const listOrgGrants = <ThrowOnError extends boolean = false>(options: Options<ListOrgGrantsData, ThrowOnError>) => (options.client ?? client).get<ListOrgGrantsResponses, ListOrgGrantsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/grants',
    ...options
});

/**
 * Grant one capability at organisation scope.
 *
 * `manage-members` held at org scope may grant capabilities the grantor
 * does not hold — the escalation path the threat model accepts, and the
 * one that keeps a fresh installation bootstrappable.
 *
 */
export const createOrgGrant = <ThrowOnError extends boolean = false>(options: Options<CreateOrgGrantData, ThrowOnError>) => (options.client ?? client).post<CreateOrgGrantResponses, CreateOrgGrantErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/grants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Apply a role template at organisation scope.
 */
export const applyOrgTemplate = <ThrowOnError extends boolean = false>(options: Options<ApplyOrgTemplateData, ThrowOnError>) => (options.client ?? client).post<ApplyOrgTemplateResponses, ApplyOrgTemplateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/grants/template',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Invite a human into this organisation with a local credential.
 *
 * The account-creation path the human-auth ADR names ("accounts are
 * created by invitation from a `manage-members` holder"). In ONE
 * transaction: a human principal and an account with the given login
 * handle are created, the optional role template is expanded into
 * grants at organisation scope exactly as `applyOrgTemplate` would
 * write them, and a credential-establishment authority is minted for
 * the new account with issuer `invitation` and the credential-reset
 * lifetime. The authority is returned once and never re-displayed:
 * hand it to the invitee out of band, who consumes it at
 * `establishCredential` and then signs in like anyone else.
 *
 * No session, no assurance and no identity link are created. An
 * invitation carries no email and no delivery channel of its own. A
 * username already in use is a `conflict`.
 *
 */
export const inviteOrgMember = <ThrowOnError extends boolean = false>(options: Options<InviteOrgMemberData, ThrowOnError>) => (options.client ?? client).post<InviteOrgMemberResponses, InviteOrgMemberErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/invitations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke one capability at project scope.
 */
export const revokeProjectGrant = <ThrowOnError extends boolean = false>(options: Options<RevokeProjectGrantData, ThrowOnError>) => (options.client ?? client).delete<RevokeProjectGrantResponses, RevokeProjectGrantErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/grants',
    ...options
});

/**
 * List the project's membership surface.
 */
export const listProjectGrants = <ThrowOnError extends boolean = false>(options: Options<ListProjectGrantsData, ThrowOnError>) => (options.client ?? client).get<ListProjectGrantsResponses, ListProjectGrantsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/grants',
    ...options
});

/**
 * Grant one capability at project scope.
 *
 * `manage-members` held at PROJECT scope may grant only capabilities the
 * grantor currently holds at or above the target scope, so a stolen
 * project-admin account is exactly the authority it was given and not
 * automatic compromise of the project's secrets.
 *
 */
export const createProjectGrant = <ThrowOnError extends boolean = false>(options: Options<CreateProjectGrantData, ThrowOnError>) => (options.client ?? client).post<CreateProjectGrantResponses, CreateProjectGrantErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/grants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Apply a role template at project scope.
 */
export const applyProjectTemplate = <ThrowOnError extends boolean = false>(options: Options<ApplyProjectTemplateData, ThrowOnError>) => (options.client ?? client).post<ApplyProjectTemplateResponses, ApplyProjectTemplateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/grants/template',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke one capability on one environment.
 */
export const revokeEnvGrant = <ThrowOnError extends boolean = false>(options: Options<RevokeEnvGrantData, ThrowOnError>) => (options.client ?? client).delete<RevokeEnvGrantResponses, RevokeEnvGrantErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/grants',
    ...options
});

/**
 * Grant one capability on one environment.
 *
 * Authorized by `manage-members(project)`, not by anything at
 * environment scope: `manage-members` is an org/project atom, so the
 * formula truncates the resolved chain to the project. There is no
 * `grant.list-env` — "who can reach this environment" must include the
 * org- and project-scoped grants that reach it, which an environment-only
 * listing would silently omit; list at org or project scope instead.
 *
 */
export const createEnvGrant = <ThrowOnError extends boolean = false>(options: Options<CreateEnvGrantData, ThrowOnError>) => (options.client ?? client).post<CreateEnvGrantResponses, CreateEnvGrantErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/grants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Apply a role template on one environment.
 */
export const applyEnvTemplate = <ThrowOnError extends boolean = false>(options: Options<ApplyEnvTemplateData, ThrowOnError>) => (options.client ?? client).post<ApplyEnvTemplateResponses, ApplyEnvTemplateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/grants/template',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read an environment's protection state and reauthentication window.
 *
 * Bare `read(E)`: an environment's protection state is part of its public
 * shape, and hiding it from a reader would make the reveal ceremony
 * inexplicable to the person subject to it.
 *
 */
export const getEnvironmentSettings = <ThrowOnError extends boolean = false>(options: Options<GetEnvironmentSettingsData, ThrowOnError>) => (options.client ?? client).get<GetEnvironmentSettingsResponses, GetEnvironmentSettingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/settings',
    ...options
});

/**
 * Set an environment's protection state and reauthentication window.
 *
 * `project-settings`, deliberately split out of `definitions-edit`: these
 * knobs exist to restrain the definitions editor, and a guard whose
 * off-switch sits in the hand it restrains is not a guard.
 *
 * Both knobs are written as ONE fact, because marking an environment
 * protected CAPS its window at the protected default. Raising a protected
 * environment's window above that cap is refused, never silently clamped.
 * Omitting `reauth_window_seconds` means "inherit the instance default";
 * a null is that same statement written out.
 *
 */
export const setEnvironmentSettings = <ThrowOnError extends boolean = false>(options: Options<SetEnvironmentSettingsData, ThrowOnError>) => (options.client ?? client).put<SetEnvironmentSettingsResponses, SetEnvironmentSettingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Export the project's canonical definitions bundle.
 */
export const exportDefinitions = <ThrowOnError extends boolean = false>(options: Options<ExportDefinitionsData, ThrowOnError>) => (options.client ?? client).get<ExportDefinitionsResponses, ExportDefinitionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/definitions/export',
    ...options
});

/**
 * Compare a definitions bundle with current project state.
 */
export const checkDefinitions = <ThrowOnError extends boolean = false>(options: Options<CheckDefinitionsData, ThrowOnError>) => (options.client ?? client).post<CheckDefinitionsResponses, CheckDefinitionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/definitions/check',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Persist an immutable definitions impact plan.
 */
export const createDefinitionsPlan = <ThrowOnError extends boolean = false>(options: Options<CreateDefinitionsPlanData, ThrowOnError>) => (options.client ?? client).post<CreateDefinitionsPlanResponses, CreateDefinitionsPlanErrors, ThrowOnError>({
    querySerializer: { parameters: { acknowledge: { array: { explode: false } } } },
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/definitions/plans',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read an immutable definitions impact plan.
 */
export const getDefinitionsPlan = <ThrowOnError extends boolean = false>(options: Options<GetDefinitionsPlanData, ThrowOnError>) => (options.client ?? client).get<GetDefinitionsPlanResponses, GetDefinitionsPlanErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/definitions/plans/{plan}',
    ...options
});

/**
 * Atomically apply a pinned definitions plan.
 */
export const applyDefinitionsPlan = <ThrowOnError extends boolean = false>(options: Options<ApplyDefinitionsPlanData, ThrowOnError>) => (options.client ?? client).post<ApplyDefinitionsPlanResponses, ApplyDefinitionsPlanErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/definitions/plans/{plan}/apply',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read definitions governance settings and last-apply provenance.
 */
export const getDefinitionsSettings = <ThrowOnError extends boolean = false>(options: Options<GetDefinitionsSettingsData, ThrowOnError>) => (options.client ?? client).get<GetDefinitionsSettingsResponses, GetDefinitionsSettingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/definitions/settings',
    ...options
});

/**
 * Select database- or Git-governed definitions.
 */
export const setDefinitionsSettings = <ThrowOnError extends boolean = false>(options: Options<SetDefinitionsSettingsData, ThrowOnError>) => (options.client ?? client).put<SetDefinitionsSettingsResponses, SetDefinitionsSettingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/definitions/settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read the per-project machine-reveal opt-in.
 *
 * The source-of-truth ADR's explicit, documented, per-project operator
 * opt-in. While it is off no machine principal may be granted `reveal`
 * in this project and every machine fetch delivers secret presence
 * only, whatever grant rows exist.
 *
 */
export const getMachineReveal = <ThrowOnError extends boolean = false>(options: Options<GetMachineRevealData, ThrowOnError>) => (options.client ?? client).get<GetMachineRevealResponses, GetMachineRevealErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/machine-reveal',
    ...options
});

/**
 * Enable or withdraw the per-project machine-reveal opt-in.
 *
 * Enabling admits `reveal` grants onto workload and automation
 * principals in this project - a standing decryption capability, and
 * on a CI runner that capability in the most-attacked box in the
 * system (machine-identities ADR). Withdrawing it stops every machine
 * secret delivery on the next fetch without touching any grant and
 * moves every machine cursor. The formula carries `reveal` because
 * nobody may confer plaintext reach they do not hold themselves, and
 * `reveal` is MFA-mandatory.
 *
 */
export const setMachineReveal = <ThrowOnError extends boolean = false>(options: Options<SetMachineRevealData, ThrowOnError>) => (options.client ?? client).put<SetMachineRevealResponses, SetMachineRevealErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/machine-reveal',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the project's key catalogue.
 *
 * The catalogue IS the project's schema, so the response carries the
 * project's monotonic schema revision beside the keys: a reader that acts
 * on this list can say which schema it saw.
 *
 */
export const listKeys = <ThrowOnError extends boolean = false>(options: Options<ListKeysData, ThrowOnError>) => (options.client ?? client).get<ListKeysResponses, ListKeysErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/keys',
    ...options
});

/**
 * Declare a key.
 *
 * The schema is closed: typing a key name that does not exist is a key
 * CREATION, an explicit act, never a silent value write. Declaration
 * well-formedness BLOCKS the save — a value may be wrong, a rule may not
 * be meaningless — so an unsupported RE2 construct, an out-of-profile
 * JSON Schema keyword, a reference cycle or a bound violation is refused
 * here rather than discovered at publish.
 *
 */
export const createKey = <ThrowOnError extends boolean = false>(options: Options<CreateKeyData, ThrowOnError>) => (options.client ?? client).post<CreateKeyResponses, CreateKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/keys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a key.
 *
 * Removes the declaration, its explicit presence rows and its group
 * membership. The group survives, possibly inert.
 *
 */
export const deleteKey = <ThrowOnError extends boolean = false>(options: Options<DeleteKeyData, ThrowOnError>) => (options.client ?? client).delete<DeleteKeyResponses, DeleteKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/keys/{key}',
    ...options
});

/**
 * Read one key declaration.
 */
export const getKey = <ThrowOnError extends boolean = false>(options: Options<GetKeyData, ThrowOnError>) => (options.client ?? client).get<GetKeyResponses, GetKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/keys/{key}',
    ...options
});

/**
 * Update a key's non-semantic metadata.
 *
 * Folder path, description, deprecation flag and deprecation note only.
 * These cannot change what any environment delivers or whether it
 * validates, so they need `definitions-edit` alone, take no reveal gate,
 * and move no schema revision.
 *
 * A body carrying `classification` is REFUSED — classification changes
 * only through the reclassification ceremony. The field is declared here
 * so the refusal names the ceremony rather than reading as an unknown
 * member.
 *
 */
export const updateKeyMetadata = <ThrowOnError extends boolean = false>(options: Options<UpdateKeyMetadataData, ThrowOnError>) => (options.client ?? client).patch<UpdateKeyMetadataResponses, UpdateKeyMetadataErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/keys/{key}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Rename a key.
 *
 * Identity is the immutable id, so a rename never breaks a reference —
 * but it DOES change the delivered payload's key set, which makes it a
 * content-affecting schema change and advances the schema revision.
 *
 */
export const renameKey = <ThrowOnError extends boolean = false>(options: Options<RenameKeyData, ThrowOnError>) => (options.client ?? client).put<RenameKeyResponses, RenameKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/keys/{key}/name',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Replace a key's value-dependent rules and presence rules.
 *
 * Changing a VALUE-DEPENDENT rule on a `secret`-classified key is a
 * disclosure: the result bit itself leaks the value, so such a change
 * additionally requires current `reveal` on that key and is rejected
 * WITHOUT EVALUATING the new declaration when the caller lacks it. The
 * refusal is the uniform nonexistent shape, because a distinguishable one
 * would itself be the oracle the gate exists to close.
 *
 * Presence rules are not value-dependent and need no reveal: they report
 * only whether an entry is set or absent.
 *
 */
export const updateKeyDeclaration = <ThrowOnError extends boolean = false>(options: Options<UpdateKeyDeclarationData, ThrowOnError>) => (options.client ?? client).put<UpdateKeyDeclarationResponses, UpdateKeyDeclarationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/keys/{key}/declaration',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The reclassification ceremony.
 *
 * The ONLY path that changes a key's classification. Declassifying
 * (`secret` to `config`) additionally requires current `reveal`: after it
 * the value is readable under ordinary environment read, and you must be
 * able to see a secret in order to stop it being one. Tightening
 * (`config` to `secret`) needs no reveal and cannot un-disclose what was
 * already served as config.
 *
 */
export const reclassifyKey = <ThrowOnError extends boolean = false>(options: Options<ReclassifyKeyData, ThrowOnError>) => (options.client ?? client).put<ReclassifyKeyResponses, ReclassifyKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/keys/{key}/classification',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Set or clear a key's group membership.
 *
 * A key belongs to at most one group; an empty `group_id` takes it out of
 * every group. Membership is coupling, and coupling is a schema change.
 *
 */
export const setKeyGroup = <ThrowOnError extends boolean = false>(options: Options<SetKeyGroupData, ThrowOnError>) => (options.client ?? client).put<SetKeyGroupResponses, SetKeyGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/keys/{key}/group',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the project's key groups.
 *
 * Each group reports its members and whether it is INERT — a group left
 * with fewer than two members couples nothing, which is surfaced rather
 * than silently repaired.
 *
 */
export const listKeyGroups = <ThrowOnError extends boolean = false>(options: Options<ListKeyGroupsData, ThrowOnError>) => (options.client ?? client).get<ListKeyGroupsResponses, ListKeyGroupsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/key-groups',
    ...options
});

/**
 * Declare a key group.
 */
export const createKeyGroup = <ThrowOnError extends boolean = false>(options: Options<CreateKeyGroupData, ThrowOnError>) => (options.client ?? client).post<CreateKeyGroupResponses, CreateKeyGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/key-groups',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a key group.
 *
 * Dissolves the coupling and releases its members; it never deletes the
 * keys it coupled.
 *
 */
export const deleteKeyGroup = <ThrowOnError extends boolean = false>(options: Options<DeleteKeyGroupData, ThrowOnError>) => (options.client ?? client).delete<DeleteKeyGroupResponses, DeleteKeyGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/key-groups/{group}',
    ...options
});

/**
 * Read one key group.
 */
export const getKeyGroup = <ThrowOnError extends boolean = false>(options: Options<GetKeyGroupData, ThrowOnError>) => (options.client ?? client).get<GetKeyGroupResponses, GetKeyGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/key-groups/{group}',
    ...options
});

/**
 * Rename a key group.
 *
 * A group name is never delivered and never validated against, so —
 * unlike a key rename — it moves no schema revision.
 *
 */
export const renameKeyGroup = <ThrowOnError extends boolean = false>(options: Options<RenameKeyGroupData, ThrowOnError>) => (options.client ?? client).patch<RenameKeyGroupResponses, RenameKeyGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/key-groups/{group}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The environment's resolved values.
 *
 * One row per DECLARED key, each `set` or `absent`. In the flat model
 * this IS the resolution - a lookup per key with nothing underneath - so
 * an absent row carries no value from any source.
 *
 */
export const listValues = <ThrowOnError extends boolean = false>(options: Options<ListValuesData, ThrowOnError>) => (options.client ?? client).get<ListValuesResponses, ListValuesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/values',
    ...options
});

/**
 * Stage a clear to `absent`.
 *
 * Stages the `set` to `absent` transition; it does not publish. With
 * nothing to inherit from, this is the whole of "remove this value now":
 * there is no layer underneath for it to fall back to.
 *
 * A key `required_in` this environment may be staged for clearing and
 * the publish that would commit it is what refuses, naming key and
 * environment. Saving is free; publish is the authority.
 *
 */
export const clearValue = <ThrowOnError extends boolean = false>(options: Options<ClearValueData, ThrowOnError>) => (options.client ?? client).delete<ClearValueResponses, ClearValueErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/values/{key}',
    ...options
});

/**
 * Read one value cell.
 */
export const getValue = <ThrowOnError extends boolean = false>(options: Options<GetValueData, ThrowOnError>) => (options.client ?? client).get<GetValueResponses, GetValueErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/values/{key}',
    ...options
});

/**
 * Stage one value edit.
 *
 * This STAGES; it does not publish. The edit lands in the caller's own
 * working state as a pending change with an immutable version id, and the
 * environment keeps delivering what it delivered until a publish names
 * that id.
 *
 * The formula is `edit` ALONE: `edit` confers no delivery power and a
 * draft is never a disclosure, so staging must not require `publish`.
 * Edit-without-publish is the propose-and-review flow.
 *
 * SAVING IS FREE. The value is not validated here, and a key that is
 * `forbidden_in` this environment may still be staged: a draft is the
 * user's scratchpad, and blocking a save pushes work in progress into
 * external notepads, which for secrets is exactly where it must not go.
 * Every one of those refusals fires at publish instead, by name.
 *
 * A key that is not declared still refuses - typing a name that does not
 * exist is a key CREATION, an explicit act elsewhere.
 *
 */
export const setValue = <ThrowOnError extends boolean = false>(options: Options<SetValueData, ThrowOnError>) => (options.client ?? client).put<SetValueResponses, SetValueErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/values/{key}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Declare one supplied value into several environments.
 *
 * Inheritance's legitimate job was "I do not want to type this four
 * times"; this is the explicit operation that does it. One plaintext the
 * caller supplies, several environments, one transaction - and no
 * relationship between the copies: editing one later changes nothing
 * elsewhere, by design.
 *
 * Authorized per DESTINATION. Holding the write formula on two of three
 * environments writes into none of them.
 *
 */
export const declareValues = <ThrowOnError extends boolean = false>(options: Options<DeclareValuesData, ThrowOnError>) => (options.client ?? client).post<DeclareValuesResponses, DeclareValuesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/values/declare',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Copy stored values into other environments (copy-to / bulk-apply).
 *
 * Server-side duplication of STORED material, which the flat model calls
 * authorized duplication and never supply. The locked formula, evaluated
 * per side and per classification:
 *
 * secret material: reveal(source) AND reveal(destination) AND publish(destination)
 * config material:   read(source) AND                          publish(destination)
 *
 * Every copy is an INDEPENDENT value: the ciphertext is never carried
 * over, the plaintext is re-sealed under the destination row's own
 * associated data, and editing the source later changes nothing
 * downstream.
 *
 * A protected destination refuses the copy unless `confirm_protected`
 * says so, by name.
 *
 */
export const copyValues = <ThrowOnError extends boolean = false>(options: Options<CopyValuesData, ThrowOnError>) => (options.client ?? client).post<CopyValuesResponses, CopyValuesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/values/copy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Compare two environments' values.
 *
 * An explicit, authorized, per-invocation comparison - NOT an ambient
 * drift signal: divergence between environments is the point of the flat
 * model, so equality is not a health signal and nothing watches it.
 *
 * Oracle rules: write-presence without the reveal gate, plaintext only
 * with it. Where both sides are `set` and at least one is unreadable,
 * `equal` is ABSENT rather than false - whether two secrets match is
 * itself material. Authorized per side, so `read` on one environment
 * says nothing about the other.
 *
 */
export const diffValues = <ThrowOnError extends boolean = false>(options: Options<DiffValuesData, ThrowOnError>) => (options.client ?? client).get<DiffValuesResponses, DiffValuesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/values/diff',
    ...options
});

/**
 * The reveal guard's state for this environment and session.
 *
 * The window gates the PROMPT, never the authorization check
 * (permission-model ADR § The reveal guard), so a client has to know
 * whether the next disclosure will prompt and which factor that prompt
 * may offer. This route answers exactly that and discloses no material,
 * which is why its formula is `read` alone: the effective window and the
 * protected flag are project settings, and the live window is the
 * caller's own session state.
 *
 * `totp_offered` is false exactly where the effective window is `0`.
 *
 * Human sessions only: a machine identity never reauthenticates, has no
 * window to report, and reaches secret material through its own fetch
 * path rather than this one.
 *
 */
export const getRevealWindow = <ThrowOnError extends boolean = false>(options: Options<GetRevealWindowData, ThrowOnError>) => (options.client ?? client).get<GetRevealWindowResponses, GetRevealWindowErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/reveal-window',
    ...options
});

/**
 * Import phase 1 - declared keys, presence, and occurrence tokens.
 *
 * The ONLY server contact an `import` phase 1 makes. It returns, for one
 * environment: the project's definitions revision, and per key its
 * classification, its declared type, whether it is `set` or `absent`, and
 * a SERVER-MINTED OPAQUE OCCURRENCE TOKEN naming the exact resolved state
 * observed.
 *
 * It is a POST because it carries a body, not because it acts:
 * `candidates` names the keys and exact declarations the run intends to
 * propose. Every one of them
 * gets a token, INCLUDING names the project does not declare yet - those
 * are exactly the keys an import is about to create, and a manifest row
 * without a server-minted token is the one row an edited manifest could
 * forge freely. An undeclared candidate answers `declared: false`, `set:
 * false` and a token naming that state.
 *
 * The token is what binds phase 1 to phase 2. A bucket label cannot do
 * the job: `set` to `set` with a changed value preserves the bucket, so a
 * bucket-checked "reviewed overwrite" would still clobber the newer
 * value. The token is computed over the value row id, which is minted per
 * write and never reused, so an edit advances it and the match fails.
 *
 * It is opaque by construction: there is nothing inside it to parse, and
 * a caller cannot construct one for a state they have not been served. An
 * unrecognized token is exactly as informative as a stale one.
 *
 * It never requires `reveal`, never compares values and never writes.
 * Human-only: `import` joins the human-only verb list.
 *
 */
export const listValueOccurrences = <ThrowOnError extends boolean = false>(options: Options<ListValueOccurrencesData, ThrowOnError>) => (options.client ?? client).post<ListValueOccurrencesResponses, ListValueOccurrencesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/values/occurrences',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Import phase 2 - a strict, human-only batch value write.
 *
 * Consumes one environment's values file. Strict closed schema: an
 * undeclared key rejects the run BY NAME rather than being created, and
 * the whole run is one transaction.
 *
 * Collisions bucket on the target environment's state. A key already
 * `set` is SKIPPED BY DEFAULT and listed by name; `overwrite` is an
 * enumerated opt-in list, never a blanket flag. Skip-by-default is what
 * makes a re-run naturally idempotent.
 *
 * `precondition` is the one declared additive input, consumed from an
 * import run manifest. When present, the server re-evaluates phase 1's
 * read formula - `read@project AND read@environment` for every environment
 * the manifest names - inside this same transaction and ON TOP of this
 * verb's own formula, and only then compares the definitions revision and
 * each written key's occurrence token. A caller lacking either read atom receives the plain
 * authorization failure and no precondition result: the precondition is
 * not an oracle. Any movement rejects those keys by name and writes
 * nothing.
 *
 * Human-only, per the locked human-only verb list.
 *
 */
export const importValues = <ThrowOnError extends boolean = false>(options: Options<ImportValuesData, ThrowOnError>) => (options.client ?? client).post<ImportValuesResponses, ImportValuesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/values/import',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The environment's values, with `secret` plaintext.
 *
 * The locked disclosure formula for current material: `read(E)` AND
 * `reveal(E)`, with reauthentication and ONE audit event per disclosed
 * key - never one row saying "revealed 40 secrets".
 *
 * It is a POST, and a route of its own rather than a flag on the read,
 * for two reasons that are the same reason: disclosure is an ACT (it
 * writes the audit records that make it accountable), and the ADR's rule
 * is one verb per disclosure path, one gate.
 *
 */
export const revealValues = <ThrowOnError extends boolean = false>(options: Options<RevealValuesData, ThrowOnError>) => (options.client ?? client).post<RevealValuesResponses, RevealValuesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/values/reveal',
    ...options
});

/**
 * Read one cell with `secret` plaintext.
 */
export const revealValue = <ThrowOnError extends boolean = false>(options: Options<RevealValueData, ThrowOnError>) => (options.client ?? client).post<RevealValueResponses, RevealValueErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/values/{key}/reveal',
    ...options
});

/**
 * Compare two environments with `secret` plaintext.
 *
 * The gated half of `values diff`. Authorized PER SIDE - `reveal` on
 * staging says nothing about prod - and one disclosure event per key per
 * side.
 *
 */
export const revealValueDiff = <ThrowOnError extends boolean = false>(options: Options<RevealValueDiffData, ThrowOnError>) => (options.client ?? client).post<RevealValueDiffResponses, RevealValueDiffErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/values/diff/reveal',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Enabled login methods for this instance.
 *
 * Public discovery: the enabled OIDC provider slugs and display names, and
 * whether local login is enabled. Instance-level, never per-account, so it
 * discloses nothing about which accounts or factors exist. Under the
 * discovery admission bucket.
 *
 */
export const authMethods = <ThrowOnError extends boolean = false>(options?: Options<AuthMethodsData, ThrowOnError>) => (options?.client ?? client).get<AuthMethodsResponses, AuthMethodsErrors, ThrowOnError>({ url: '/api/v1/auth/methods', ...options });

/**
 * Begin an OIDC transaction (login, link or reauth).
 *
 * Creates a single-use server-side transaction (PKCE S256 always) and
 * returns the IdP authorization URL. `login` is anonymous and bound by a
 * `__Host-` browser-binding cookie set on this response (A2/A16); `link`
 * and `reauth` require an authenticated session and are session-bound, and
 * `link` verifies the account-security proof up front (A6).
 *
 */
export const oidcStart = <ThrowOnError extends boolean = false>(options: Options<OidcStartData, ThrowOnError>) => (options.client ?? client).post<OidcStartResponses, OidcStartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/oidc/{provider}/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Complete an OIDC transaction from the IdP redirect.
 *
 * Per-provider distinct callback path (mix-up defence leg 1): the path
 * provider must equal the transaction's provider, refused BEFORE any token
 * is fetched (A1). The transaction is consumed single-use, the code is
 * exchanged only at the recorded provider, the ID token validated
 * completely, and the purpose wall is structural. Returns the minted or
 * rotated session.
 *
 */
export const oidcCallback = <ThrowOnError extends boolean = false>(options: Options<OidcCallbackData, ThrowOnError>) => (options.client ?? client).get<OidcCallbackResponses, OidcCallbackErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/oidc/{provider}/callback',
    ...options
});

/**
 * Begin an SP-initiated SAML transaction (login, link or reauth).
 *
 * Mints the server-side request, RelayState and initiator binding before
 * returning the IdP HTTP-Redirect URL. `login` is anonymous; `link` and
 * `reauth` require and bind the acting human session. The initiator cookie
 * is delivered only as a Secure, HttpOnly, SameSite=None cookie scoped to
 * this provider's ACS path; it is never returned in JSON.
 *
 */
export const samlStart = <ThrowOnError extends boolean = false>(options: Options<SamlStartData, ThrowOnError>) => (options.client ?? client).post<SamlStartResponses, SamlStartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/saml/{provider}/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Complete an SP-initiated SAML transaction over HTTP-POST.
 *
 * The only accepted response binding. The per-provider path, exact
 * Destination, both Issuer legs, RelayState, InResponseTo and initiator
 * binding must all resolve to the same live transaction before an
 * ordinary session can be minted, linked or reauthenticated.
 *
 */
export const samlAcs = <ThrowOnError extends boolean = false>(options: Options<SamlAcsData, ThrowOnError>) => (options.client ?? client).post<SamlAcsResponses, SamlAcsErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/api/v1/auth/saml/{provider}/acs',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Publish this provider's SAML SP metadata.
 *
 * Documentation-class public material: entityID, this provider's ACS,
 * the active and retiring SP signing certificates, and the signed-request
 * policy. Served under pre-authentication admission controls.
 *
 */
export const samlMetadata = <ThrowOnError extends boolean = false>(options: Options<SamlMetadataData, ThrowOnError>) => (options.client ?? client).get<SamlMetadataResponses, SamlMetadataErrors, ThrowOnError>({ url: '/api/v1/auth/saml/{provider}/metadata', ...options });

/**
 * The organisations the caller's own grants name.
 *
 * The navigation surface: exactly the organisations in which the calling
 * principal holds at least one grant, at org scope or below.
 *
 * This is **not** `listOrgs`. That one enumerates every organisation on
 * the instance and is operator work under `instance-config`, which the
 * human-auth ADR makes MFA-mandatory; using it for navigation would put
 * a second-factor wall in front of a sidebar. This one is a projection
 * of the caller's *own* grant rows — disclosure of one's own grants is
 * already presupposed by the grant views — so it needs no capability and
 * can leak nothing the caller does not already hold.
 *
 * A principal whose only grant is **instance-scoped** gets an EMPTY
 * list, and that is correct rather than a gap. Instance scope inherits
 * downward, so expanding it here would reproduce `listOrgs` without its
 * gate. An operator who wants the whole instance asks for the whole
 * instance, and steps up to do it.
 *
 * Identity only — id and name. An organisation's `metadata` is
 * operator-set and is read through `getOrg`, which authorizes.
 *
 */
export const listMyOrgs = <ThrowOnError extends boolean = false>(options?: Options<ListMyOrgsData, ThrowOnError>) => (options?.client ?? client).get<ListMyOrgsResponses, ListMyOrgsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me/orgs',
    ...options
});

/**
 * List the caller's linked external identities.
 */
export const listIdentities = <ThrowOnError extends boolean = false>(options?: Options<ListIdentitiesData, ThrowOnError>) => (options?.client ?? client).get<ListIdentitiesResponses, ListIdentitiesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/identities',
    ...options
});

/**
 * Begin linking an external identity (OIDC purpose=link).
 *
 * An account-security mutation: verifies the pre-existing password up
 * front and starts an OIDC link transaction bound to the acting session.
 * Returns the IdP authorization URL; the link lands at the callback.
 *
 */
export const linkIdentity = <ThrowOnError extends boolean = false>(options: Options<LinkIdentityData, ThrowOnError>) => (options.client ?? client).post<LinkIdentityResponses, LinkIdentityErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/identities/link',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unlink an external identity.
 *
 * An account-security mutation proved by the pre-existing password. Refuses
 * removing the last remaining credential; deletes the identity's sessions;
 * reissues the acting session. The reissued token replaces the old one.
 *
 */
export const unlinkIdentity = <ThrowOnError extends boolean = false>(options: Options<UnlinkIdentityData, ThrowOnError>) => (options.client ?? client).delete<UnlinkIdentityResponses, UnlinkIdentityErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/identities/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Issue a credential-establishment authority for another account.
 *
 * Administrator-issued credential reset (human-auth ADR § Recovery). A
 * credential-reset holder mints a single-use, hashed, expiring
 * credential-establishment authority for a target, returned once and
 * transmitted out of band. The reset advances the target's session
 * generation and revokes its sessions in the same transaction.
 *
 * Org-scoped credential-reset reaches only a target whose grants lie
 * entirely within that org and who holds no instance capability; the
 * org-bounded test is evaluated in one serializable transaction with the
 * target's grant set (a concurrent grant landing conflicts with the
 * in-flight reset). A target holding an instance capability has no network
 * reset path at all — break-glass only. This route dispatches between the
 * org-scoped and instance-scoped credential-reset operations by that
 * classification. Every failure that could enumerate the
 * target — an unknown or non-human principal, a caller lacking
 * credential-reset, an org-P holder reaching an org-O target, and a target
 * holding an instance capability (break-glass only) — answers a single
 * uniform `401`; the true cause is durable in the audit trail, never on the
 * wire.
 *
 */
export const resetCredential = <ThrowOnError extends boolean = false>(options: Options<ResetCredentialData, ThrowOnError>) => (options.client ?? client).post<ResetCredentialResponses, ResetCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/accounts/{principal}/credential-reset',
    ...options
});

/**
 * Begin passkey enrolment; returns opaque creation options.
 *
 * Verifies the account-security proof up front (the pre-existing password
 * or a confirmed TOTP code, where one stands) and opens a registration
 * ceremony, returning the browser-generated creation options verbatim.
 * `enrol/finish` completes the enrolment.
 *
 */
export const enrolPasskeyStart = <ThrowOnError extends boolean = false>(options: Options<EnrolPasskeyStartData, ThrowOnError>) => (options.client ?? client).post<EnrolPasskeyStartResponses, EnrolPasskeyStartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/enrol/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Complete passkey enrolment from the attestation response.
 *
 * Validates the opaque attestation response against the live ceremony,
 * stores the credential, and reissues the acting session. A browser
 * session's reissued token is delivered on the `__Host-hikyo` cookie.
 *
 */
export const enrolPasskeyFinish = <ThrowOnError extends boolean = false>(options: Options<EnrolPasskeyFinishData, ThrowOnError>) => (options.client ?? client).post<EnrolPasskeyFinishResponses, EnrolPasskeyFinishErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/enrol/finish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Begin a discoverable-credential passkey login.
 *
 * Fully pre-auth: opens a discoverable-credential request ceremony and
 * returns the opaque request options. Discloses nothing about which
 * accounts or credentials exist.
 *
 */
export const passkeyLoginStart = <ThrowOnError extends boolean = false>(options?: Options<PasskeyLoginStartData, ThrowOnError>) => (options?.client ?? client).post<PasskeyLoginStartResponses, PasskeyLoginStartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/login/start',
    ...options
});

/**
 * Complete a passkey login from the assertion response.
 *
 * Validates the opaque assertion against the live ceremony and mints a
 * browser session carrying WebAuthn assurance, delivered on the
 * `__Host-hikyo` cookie. A signature-count regression disables the cloned
 * credential (audited) and refuses.
 *
 */
export const passkeyLoginFinish = <ThrowOnError extends boolean = false>(options: Options<PasskeyLoginFinishData, ThrowOnError>) => (options.client ?? client).post<PasskeyLoginFinishResponses, PasskeyLoginFinishErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/login/finish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Begin a passkey step-up for the acting session.
 *
 * Opens a non-discoverable ceremony scoped to the acting account's
 * credentials and returns opaque request options. `step-up/finish`
 * elevates the session.
 *
 */
export const stepUpPasskeyStart = <ThrowOnError extends boolean = false>(options?: Options<StepUpPasskeyStartData, ThrowOnError>) => (options?.client ?? client).post<StepUpPasskeyStartResponses, StepUpPasskeyStartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/step-up/start',
    ...options
});

/**
 * Elevate the acting session by presenting a passkey.
 *
 * Validates the opaque assertion and appends `webauthn` to the acting
 * session's factor set, rotating its token (delivered on the `__Host-hikyo`
 * cookie for a browser session). The original authenticated_at is
 * preserved.
 *
 */
export const stepUpPasskeyFinish = <ThrowOnError extends boolean = false>(options: Options<StepUpPasskeyFinishData, ThrowOnError>) => (options.client ?? client).post<StepUpPasskeyFinishResponses, StepUpPasskeyFinishErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/step-up/finish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Begin a passkey reauthentication bound to an operation unit.
 *
 * Opens a ceremony whose challenge is bound to the enumerated unit
 * (environment plus the sorted credential ids), so the assertion
 * authorizes exactly that unit. `reauth/finish` opens the window.
 *
 */
export const reauthPasskeyStart = <ThrowOnError extends boolean = false>(options: Options<ReauthPasskeyStartData, ThrowOnError>) => (options.client ?? client).post<ReauthPasskeyStartResponses, ReauthPasskeyStartErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/reauth/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Complete a passkey reauthentication; opens the window.
 *
 * Validates the opaque assertion and opens a reauthentication window over
 * the bound environment (single-decision where the effective window is
 * zero). The reauth rotates the acting session; a browser session's
 * rotated token is delivered on the `__Host-hikyo` cookie.
 *
 */
export const reauthPasskeyFinish = <ThrowOnError extends boolean = false>(options: Options<ReauthPasskeyFinishData, ThrowOnError>) => (options.client ?? client).post<ReauthPasskeyFinishResponses, ReauthPasskeyFinishErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/reauth/finish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the caller's enrolled passkeys.
 */
export const listPasskeys = <ThrowOnError extends boolean = false>(options?: Options<ListPasskeysData, ThrowOnError>) => (options?.client ?? client).get<ListPasskeysResponses, ListPasskeysErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/credentials',
    ...options
});

/**
 * Remove an enrolled passkey.
 *
 * An account-security mutation proved by the pre-existing password or a
 * TOTP code (never the credential being removed, B7). Refuses a removal
 * that would drop a passwordless account below the passkey-only floor;
 * reissues the acting session. A browser session's reissued token is
 * delivered on the `__Host-hikyo` cookie.
 *
 */
export const removePasskey = <ThrowOnError extends boolean = false>(options: Options<RemovePasskeyData, ThrowOnError>) => (options.client ?? client).delete<RemovePasskeyResponses, RemovePasskeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/webauthn/credentials/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List configured OIDC providers.
 */
export const listOidcProviders = <ThrowOnError extends boolean = false>(options?: Options<ListOidcProvidersData, ThrowOnError>) => (options?.client ?? client).get<ListOidcProvidersResponses, ListOidcProvidersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/oidc-providers',
    ...options
});

/**
 * Delete an OIDC provider.
 *
 * Sweeps every session authenticated through the provider (A4); its live
 * transactions cascade (A14).
 *
 */
export const deleteOidcProvider = <ThrowOnError extends boolean = false>(options: Options<DeleteOidcProviderData, ThrowOnError>) => (options.client ?? client).delete<DeleteOidcProviderResponses, DeleteOidcProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/oidc-providers/{slug}',
    ...options
});

/**
 * Read one OIDC provider.
 */
export const getOidcProvider = <ThrowOnError extends boolean = false>(options: Options<GetOidcProviderData, ThrowOnError>) => (options.client ?? client).get<GetOidcProviderResponses, GetOidcProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/oidc-providers/{slug}',
    ...options
});

/**
 * Create or reconfigure an OIDC provider.
 *
 * The issuer is immutable after create (A3): a changed issuer is refused.
 * Discovery is re-run and the document's issuer must byte-equal the
 * configured one. A reconfigure sweeps every session authenticated through
 * the provider (A4).
 *
 */
export const putOidcProvider = <ThrowOnError extends boolean = false>(options: Options<PutOidcProviderData, ThrowOnError>) => (options.client ?? client).put<PutOidcProviderResponses, PutOidcProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/oidc-providers/{slug}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read payload-pruner health.
 */
export const getRetentionHealth = <ThrowOnError extends boolean = false>(options?: Options<GetRetentionHealthData, ThrowOnError>) => (options?.client ?? client).get<GetRetentionHealthResponses, GetRetentionHealthErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/retention-health',
    ...options
});

/**
 * Read the configured release channel and available update.
 */
export const getUpdateStatus = <ThrowOnError extends boolean = false>(options?: Options<GetUpdateStatusData, ThrowOnError>) => (options?.client ?? client).get<GetUpdateStatusResponses, GetUpdateStatusErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/update-status',
    ...options
});

/**
 * Refuse legacy remote apply while preserving authenticated audit evidence.
 *
 * Remote apply is disabled because the legacy updater cannot prove safe
 * rollback after schema writes. No job is submitted or resumed. An
 * authorized request returns 409 with an explicit unsupported reason.
 * The caller must still hold instance-config, have multi-factor assurance,
 * and have authenticated within the last five minutes. This endpoint
 * remains present for older clients; release metadata and manual verified
 * downloads remain available. The 202 shape is retained for wire
 * compatibility with historical helper responses, not current enablement.
 *
 */
export const requestInstanceUpdate = <ThrowOnError extends boolean = false>(options: Options<RequestInstanceUpdateData, ThrowOnError>) => (options.client ?? client).post<RequestInstanceUpdateResponses, RequestInstanceUpdateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/update',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read one local updater job and reconcile its terminal audit outcome.
 */
export const getInstanceUpdateJob = <ThrowOnError extends boolean = false>(options: Options<GetInstanceUpdateJobData, ThrowOnError>) => (options.client ?? client).get<GetInstanceUpdateJobResponses, GetInstanceUpdateJobErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/updates/{job}',
    ...options
});

/**
 * List configured SAML providers.
 */
export const listSamlProviders = <ThrowOnError extends boolean = false>(options?: Options<ListSamlProvidersData, ThrowOnError>) => (options?.client ?? client).get<ListSamlProvidersResponses, ListSamlProvidersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-providers',
    ...options
});

/**
 * Remove a SAML provider.
 *
 * Deletes every session authenticated through this provider.
 */
export const deleteSamlProvider = <ThrowOnError extends boolean = false>(options: Options<DeleteSamlProviderData, ThrowOnError>) => (options.client ?? client).delete<DeleteSamlProviderResponses, DeleteSamlProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-providers/{slug}',
    ...options
});

/**
 * Read one SAML provider.
 */
export const getSamlProvider = <ThrowOnError extends boolean = false>(options: Options<GetSamlProviderData, ThrowOnError>) => (options.client ?? client).get<GetSamlProviderResponses, GetSamlProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-providers/{slug}',
    ...options
});

/**
 * Partially update SAML provider policy or disable the provider.
 *
 * Omitted members preserve their stored values. Metadata, entityID and
 * certificate changes cannot ride this path: they remain behind the
 * metadata diff-and-confirm ceremony. Disabling sweeps this provider's
 * sessions. This additive endpoint supports the locked `update` and
 * `disable` CLI spellings without requiring unreadable metadata input.
 *
 */
export const patchSamlProvider = <ThrowOnError extends boolean = false>(options: Options<PatchSamlProviderData, ThrowOnError>) => (options.client ?? client).patch<PatchSamlProviderResponses, PatchSamlProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-providers/{slug}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create or reconfigure a SAML provider from pinned metadata.
 *
 * The configured entityID selects exactly one metadata descriptor and is
 * immutable after create. File and one-shot URL sources enter the same
 * diff-and-confirm ceremony;
 * certificate, endpoint and assurance-policy changes sweep this
 * provider's sessions. SAML never provisions accounts, so no provisioning member
 * exists in this request schema.
 *
 */
export const putSamlProvider = <ThrowOnError extends boolean = false>(options: Options<PutSamlProviderData, ThrowOnError>) => (options.client ?? client).put<PutSamlProviderResponses, PutSamlProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-providers/{slug}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Refresh and explicitly confirm a SAML provider's metadata diff.
 *
 * Performs one URL fetch for URL-backed providers. Any new endpoint,
 * assertion-signing certificate or metadata-signing fingerprint remains
 * refused until named in `confirmed_fingerprints` and committed through
 * this action. File-backed providers supply the replacement document.
 *
 */
export const refreshSamlProviderMetadata = <ThrowOnError extends boolean = false>(options: Options<RefreshSamlProviderMetadataData, ThrowOnError>) => (options.client ?? client).post<RefreshSamlProviderMetadataResponses, RefreshSamlProviderMetadataErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-providers/{slug}/refresh-metadata',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List SAML SP signing-key lifecycle state.
 *
 * Returns fingerprints and public lifecycle metadata only; private key material never leaves the server.
 */
export const listSamlSpKeys = <ThrowOnError extends boolean = false>(options?: Options<ListSamlSpKeysData, ThrowOnError>) => (options?.client ?? client).get<ListSamlSpKeysResponses, ListSamlSpKeysErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-sp-keys',
    ...options
});

/**
 * Rotate the active SAML SP signing key with an overlap window.
 *
 * Atomically marks the old active key retiring and publishes a new active key; both certificates remain in SP metadata until explicit retirement.
 */
export const rotateSamlSpKey = <ThrowOnError extends boolean = false>(options?: Options<RotateSamlSpKeyData, ThrowOnError>) => (options?.client ?? client).post<RotateSamlSpKeyResponses, RotateSamlSpKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-sp-keys/rotate',
    ...options
});

/**
 * Retire and erase an overlap-retiring SAML SP key.
 *
 * Refuses the active key; rotate first so request-signing continuity is explicit.
 */
export const retireSamlSpKey = <ThrowOnError extends boolean = false>(options: Options<RetireSamlSpKeyData, ThrowOnError>) => (options.client ?? client).delete<RetireSamlSpKeyResponses, RetireSamlSpKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-sp-keys/{fingerprint}',
    ...options
});

/**
 * Immediately erase and replace a compromised active SAML SP key.
 *
 * Atomically removes the named active key and mints its replacement without publishing an overlap window.
 */
export const compromiseRetireSamlSpKey = <ThrowOnError extends boolean = false>(options: Options<CompromiseRetireSamlSpKeyData, ThrowOnError>) => (options.client ?? client).post<CompromiseRetireSamlSpKeyResponses, CompromiseRetireSamlSpKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/saml-sp-keys/{fingerprint}/compromise-retire',
    ...options
});

/**
 * List the project's service accounts.
 *
 * Metadata only, and listing is a narrowing-class operation: plain
 * `manage-identities`, no disclosure gate, because incident response
 * must never be gated on disclosure rights.
 *
 */
export const listServiceAccounts = <ThrowOnError extends boolean = false>(options: Options<ListServiceAccountsData, ThrowOnError>) => (options.client ?? client).get<ListServiceAccountsResponses, ListServiceAccountsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/service-accounts',
    ...options
});

/**
 * Create a project-owned machine principal.
 *
 * `kind` is declared here and is IMMUTABLE. A project that guessed wrong
 * creates a second service account; there is no in-place widening of a
 * credential class that N workloads already hold.
 *
 */
export const createServiceAccount = <ThrowOnError extends boolean = false>(options: Options<CreateServiceAccountData, ThrowOnError>) => (options.client ?? client).post<CreateServiceAccountResponses, CreateServiceAccountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/service-accounts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a service account, revoking every credential and grant.
 *
 * One transaction: every credential is revoked and every grant released.
 * Plain `manage-identities`, no disclosure gate — requiring `reveal` to
 * deprovision a compromised workload would be a self-inflicted
 * incident-response delay.
 *
 */
export const deleteServiceAccount = <ThrowOnError extends boolean = false>(options: Options<DeleteServiceAccountData, ThrowOnError>) => (options.client ?? client).delete<DeleteServiceAccountResponses, DeleteServiceAccountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/service-accounts/{serviceAccount}',
    ...options
});

/**
 * List a service account's credentials, metadata only.
 *
 * Never the value. A credential value is displayed exactly once, at
 * mint, and no route in this contract returns one afterwards.
 *
 */
export const listMachineCredentials = <ThrowOnError extends boolean = false>(options: Options<ListMachineCredentialsData, ThrowOnError>) => (options.client ?? client).get<ListMachineCredentialsResponses, ListMachineCredentialsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/service-accounts/{serviceAccount}/credentials',
    ...options
});

/**
 * Mint a bearer credential, returned exactly once.
 *
 * The full authorization formula is `manage-identities(project)` AND a
 * disclosure capability over EVERY environment reachable in the
 * resulting post-state AND reauthentication. The post-state phrasing is
 * load-bearing: a replacement credential adds no environment, so an
 * "added environments" reading would let a principal holding
 * `manage-identities` and no `reveal` rotate a production credential and
 * walk away with a live production-reading token. The two disclosure
 * classes are evaluated independently — `reveal` over newly reachable
 * current plaintext, `reveal-history` over historical.
 *
 * Rotation is this call followed by a revoke of the predecessor:
 * overlap is the intended steady state, so a mint that succeeds and a
 * revoke that fails leaves two live credentials rather than none.
 *
 */
export const mintMachineCredential = <ThrowOnError extends boolean = false>(options: Options<MintMachineCredentialData, ThrowOnError>) => (options.client ?? client).post<MintMachineCredentialResponses, MintMachineCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/service-accounts/{serviceAccount}/credentials',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke one credential.
 *
 * Bites at the next request: the liveness predicate is read in the
 * authenticating transaction, uncached. Revoking one credential is not
 * deprovisioning — grants are untouched and siblings keep working.
 *
 */
export const revokeMachineCredential = <ThrowOnError extends boolean = false>(options: Options<RevokeMachineCredentialData, ThrowOnError>) => (options.client ?? client).delete<RevokeMachineCredentialResponses, RevokeMachineCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/service-accounts/{serviceAccount}/credentials/{credential}',
    ...options
});

/**
 * Read the instance credential lifetime controls.
 */
export const getCredentialPolicy = <ThrowOnError extends boolean = false>(options?: Options<GetCredentialPolicyData, ThrowOnError>) => (options?.client ?? client).get<GetCredentialPolicyResponses, GetCredentialPolicyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/credential-policy',
    ...options
});

/**
 * Move the instance credential lifetime controls.
 *
 * The two controls are separate on purpose: raising
 * `max_finite_lifetime_seconds` can never manufacture an indefinite
 * credential. A TIGHTENING enumerates every affected live credential and
 * refuses with 409 until the caller re-sends with `confirm: true`, so a
 * settings change never silently kills a live credential.
 *
 */
export const setCredentialPolicy = <ThrowOnError extends boolean = false>(options: Options<SetCredentialPolicyData, ThrowOnError>) => (options.client ?? client).put<SetCredentialPolicyResponses, SetCredentialPolicyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/credential-policy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the configured OIDC federation issuers.
 *
 * Instance-scoped, never org- or project-scoped: an org-scoped issuer would
 * let an org admin add a provider and mint identities authenticating into
 * the instance.
 *
 * Audited rather than silent — reading which external authorities the
 * instance trusts to name principals is not a bare tenant read.
 *
 */
export const listFederationIssuers = <ThrowOnError extends boolean = false>(options?: Options<ListFederationIssuersData, ThrowOnError>) => (options?.client ?? client).get<ListFederationIssuersResponses, ListFederationIssuersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/federation-issuers',
    ...options
});

/**
 * Configure an OIDC federation issuer.
 *
 * The issuer string is stored and matched BYTE-EXACT, with no
 * canonicalization step. OpenID Connect defines `iss` as case-sensitive, so
 * any normalization that folds case, resolves a URL or strips a trailing
 * slash can merge two distinct external issuers into one configuration —
 * and the uniqueness constraint would enforce the merge rather than catch
 * it.
 *
 */
export const createFederationIssuer = <ThrowOnError extends boolean = false>(options: Options<CreateFederationIssuerData, ThrowOnError>) => (options.client ?? client).post<CreateFederationIssuerResponses, CreateFederationIssuerErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/federation-issuers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove an issuer configuration.
 *
 * Refused with 409 while live bindings still name it. A cascade would
 * deprovision N workloads under an operation whose name says
 * "configuration", and an orphaning delete would leave bindings that
 * silently stop authenticating. The operator revokes the bindings first,
 * which is the same act stated honestly.
 *
 */
export const deleteFederationIssuer = <ThrowOnError extends boolean = false>(options: Options<DeleteFederationIssuerData, ThrowOnError>) => (options.client ?? client).delete<DeleteFederationIssuerResponses, DeleteFederationIssuerErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/federation-issuers/{issuer}',
    ...options
});

/**
 * Move an issuer's JWKS source or refused audiences.
 *
 * The mutable half only. The issuer string and the platform type cannot
 * move: changing either would silently re-point every binding underneath at
 * a different external authority, which is a replacement, not an edit.
 *
 */
export const updateFederationIssuer = <ThrowOnError extends boolean = false>(options: Options<UpdateFederationIssuerData, ThrowOnError>) => (options.client ?? client).patch<UpdateFederationIssuerResponses, UpdateFederationIssuerErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/federation-issuers/{issuer}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Mint a federated `(issuer, subject)` binding. Nothing at rest.
 *
 * The full authorization formula is `manage-identities(project)` AND a
 * disclosure capability over EVERY environment reachable in the resulting
 * post-state AND reauthentication — the same formula a bearer mint carries,
 * and for the same reason: the actor decides which external identity may
 * speak as this service account, and that identity reaches everything the
 * account's grants reach.
 *
 * There is no PUT and no PATCH. Bindings are IMMUTABLE; a change is a
 * replacement mint through this route naming `replaces`, so the predecessor
 * is revoked and the successor inserted in one transaction. Editing in place
 * would let a principal without `reveal` re-point a production binding at an
 * identity they control while the recorded authorization stayed behind.
 *
 * Bindings are LISTED and DELETED through the credentials routes: a binding
 * is a credential row, so a second pair of routes over the same rows would
 * be two places for one formula to drift.
 *
 */
export const createFederatedBinding = <ThrowOnError extends boolean = false>(options: Options<CreateFederatedBindingData, ThrowOnError>) => (options.client ?? client).post<CreateFederatedBindingResponses, CreateFederatedBindingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/service-accounts/{serviceAccount}/bindings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Fetch the authorized projection, conditionally.
 *
 * The machine fetch surface. Presenting a current cursor answers
 * `current: true` with NO content and no key names — only a fetch that
 * actually delivers values is a disclosure — and either disposition emits
 * exactly one immutable access record, plus one immutable disclosure
 * record per delivered value.
 *
 * Authorization is evaluated on the conditional path EXACTLY as on the
 * delivering path, so a caller who has lost `read` receives the uniform
 * nonexistent response rather than "current".
 *
 * What a key delivers is decided server-side, per key, in-transaction: a
 * `config` key carries its plaintext under `read`; a `secret` key carries
 * its plaintext under `read ∧ reveal` (or `read ∧ reveal-history` for a
 * pinned non-current revision), and otherwise arrives presence-only with
 * no value. This is the surface the Compose and Kubernetes integrations
 * consume to converge their delivery targets.
 *
 * Both a `hik_` bearer credential and an externally issued OIDC ID token
 * authenticate here, with identical authority: a machine principal's
 * authority is the union of the grants on its service account and nothing
 * else, so there is no per-credential scope for either kind to widen.
 * Human sessions are refused at artifact admission with the uniform
 * nonexistent response.
 *
 */
export const fetchDelivery = <ThrowOnError extends boolean = false>(options: Options<FetchDeliveryData, ThrowOnError>) => (options.client ?? client).get<FetchDeliveryResponses, FetchDeliveryErrors, ThrowOnError>({
    querySerializer: { parameters: { acknowledged_keys: { array: { explode: false } } } },
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/delivery',
    ...options
});

/**
 * Reconcile client-durable offline disclosure records.
 *
 * Accepts records fsynced by a client before offline plaintext release.
 * Idempotency is scoped to the presenting principal and record id. A live
 * credential may reconcile records served by a credential since revoked.
 *
 */
export const reconcileOfflineRecords = <ThrowOnError extends boolean = false>(options: Options<ReconcileOfflineRecordsData, ThrowOnError>) => (options.client ?? client).post<ReconcileOfflineRecordsResponses, ReconcileOfflineRecordsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/delivery/offline-records',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List this organisation's SCIM bindings.
 *
 * Each binding with its attention states — the org admin's answer to
 * "what does SCIM think is wrong?". Provider availability and staleness
 * are reconciled on read, in both directions, so the view is current
 * rather than as of the last write.
 *
 */
export const listScimBindings = <ThrowOnError extends boolean = false>(options: Options<ListScimBindingsData, ThrowOnError>) => (options.client ?? client).get<ListScimBindingsResponses, ListScimBindingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings',
    ...options
});

/**
 * Create the organisation's SCIM binding for one identity provider.
 *
 * At most one binding per (organisation, provider): the concurrent-create
 * race resolves to one row and the loser is refused with `conflict`. The
 * subject source and the NameID profile are immutable after creation, and
 * the provider reference is read-only — creating a binding grants no
 * authority over the provider it names. Creating the binding also creates
 * its provisioning connection and that connection's structural
 * `scim-provision` grant, atomically.
 *
 */
export const createScimBinding = <ThrowOnError extends boolean = false>(options: Options<CreateScimBindingData, ThrowOnError>) => (options.client ?? client).post<CreateScimBindingResponses, CreateScimBindingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a SCIM binding, running its atomic teardown.
 *
 * One transaction, in order: every credential is revoked so no new wire
 * transaction can begin; every `scim` origin the binding holds is
 * released (lockout conversion included, and any resulting retention
 * origins survive the binding); the provisioning connection and its
 * structural grant are retired; the directory, mapping table and binding
 * row go. Identity links and accounts SURVIVE — they are account
 * property, exactly as they would be had the user been invited.
 *
 */
export const deleteScimBinding = <ThrowOnError extends boolean = false>(options: Options<DeleteScimBindingData, ThrowOnError>) => (options.client ?? client).delete<DeleteScimBindingResponses, DeleteScimBindingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}',
    ...options
});

/**
 * Read one SCIM binding and its attention states.
 *
 * Deliberately keeps working while the referenced provider is disabled or
 * removed: the whole wire surface fails closed in that state, and "state
 * is preserved for repair" is not preserved by a surface that refuses to
 * show the state.
 *
 */
export const getScimBinding = <ThrowOnError extends boolean = false>(options: Options<GetScimBindingData, ThrowOnError>) => (options.client ?? client).get<GetScimBindingResponses, GetScimBindingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}',
    ...options
});

/**
 * Delete an addressed mapping row and release every origin it holds.
 *
 * Reconciles in this transaction, with no round-trip to the identity
 * provider: the org administrator is never stuck behind an IdP they do
 * not control. The row is addressed by query parameters because the
 * triple is an address, and query parameters are the shape every
 * intermediary handles the same way.
 *
 */
export const deleteScimMapping = <ThrowOnError extends boolean = false>(options: Options<DeleteScimMappingData, ThrowOnError>) => (options.client ?? client).delete<DeleteScimMappingResponses, DeleteScimMappingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/mappings',
    ...options
});

/**
 * List the binding's group-to-template mapping table.
 *
 * Each row with the capabilities it expands into, so a reader does not
 * have to know the template table by heart, and with its `inert` flag —
 * a row whose group was deleted at the identity provider grants nothing
 * until a human edits or deletes it.
 *
 */
export const listScimMappings = <ThrowOnError extends boolean = false>(options: Options<ListScimMappingsData, ThrowOnError>) => (options.client ?? client).get<ListScimMappingsResponses, ListScimMappingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/mappings',
    ...options
});

/**
 * Add a mapping row, granting the group's current members immediately.
 *
 * The blast-radius moment. The locked rule demands the org-scope warning
 * "at grant time", when a human is present — under SCIM no human attends
 * the sync, so the consequence language is returned HERE, in the same
 * transaction that creates the origins for every member the group already
 * has. There is no "waits for the next push" gap, and the `warnings`
 * array is part of the response rather than an optional decoration.
 *
 */
export const createScimMapping = <ThrowOnError extends boolean = false>(options: Options<CreateScimMappingData, ThrowOnError>) => (options.client ?? client).post<CreateScimMappingResponses, CreateScimMappingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/mappings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Retarget an addressed mapping row's template.
 *
 * The row is addressed by (group, scope) and keeps its id, which is what
 * makes narrowing precise: origins key on the row, so a delete-and-
 * recreate would release every origin it holds and immediately recreate
 * most of them — momentarily revoking capabilities that never stopped
 * being granted. Widening creates the newly covered origins now;
 * narrowing releases the no-longer-covered part in this same transaction,
 * with no round-trip to the identity provider.
 *
 */
export const updateScimMapping = <ThrowOnError extends boolean = false>(options: Options<UpdateScimMappingData, ThrowOnError>) => (options.client ?? client).put<UpdateScimMappingResponses, UpdateScimMappingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/mappings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the binding's provisioning credentials.
 *
 * Ids and metadata, never token material — which does not exist to be
 * listed, because only the verifier was ever stored.
 *
 */
export const listScimCredentials = <ThrowOnError extends boolean = false>(options: Options<ListScimCredentialsData, ThrowOnError>) => (options.client ?? client).get<ListScimCredentialsResponses, ListScimCredentialsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/credentials',
    ...options
});

/**
 * Mint a NEW provisioning credential; several may be live at once.
 *
 * Display-once: the token is in this response and nowhere else, ever.
 * Overlap rotation is mint-new, update the identity provider, revoke-old,
 * with identical authority throughout — so a mint that joins an
 * already-live credential reports `rotated` and there is no third verb
 * and no offline window.
 *
 */
export const mintScimCredential = <ThrowOnError extends boolean = false>(options: Options<MintScimCredentialData, ThrowOnError>) => (options.client ?? client).post<MintScimCredentialResponses, MintScimCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/credentials',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke one provisioning credential.
 *
 * Bites at the next request. The row is marked rather than deleted, so
 * the verifier stays occupied and the id keeps naming a real thing here.
 *
 */
export const revokeScimCredential = <ThrowOnError extends boolean = false>(options: Options<RevokeScimCredentialData, ThrowOnError>) => (options.client ?? client).delete<RevokeScimCredentialResponses, RevokeScimCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/credentials/{id}',
    ...options
});

/**
 * Read one provisioning credential's metadata.
 */
export const getScimCredential = <ThrowOnError extends boolean = false>(options: Options<GetScimCredentialData, ThrowOnError>) => (options.client ?? client).get<GetScimCredentialResponses, GetScimCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/credentials/{id}',
    ...options
});

/**
 * The provisioned user directory, with per-user attention flags.
 *
 * Carries the loud per-user flag a deprovision raises: the identity
 * provider declared this human gone, and grants made by hand in this org
 * remain and are still usable. That flag exists precisely because a human
 * must decide about the manual remainder.
 *
 */
export const listScimDirectoryUsers = <ThrowOnError extends boolean = false>(options: Options<ListScimDirectoryUsersData, ThrowOnError>) => (options.client ?? client).get<ListScimDirectoryUsersResponses, ListScimDirectoryUsersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/directory/users',
    ...options
});

/**
 * The provisioned group directory.
 */
export const listScimDirectoryGroups = <ThrowOnError extends boolean = false>(options: Options<ListScimDirectoryGroupsData, ThrowOnError>) => (options.client ?? client).get<ListScimDirectoryGroupsResponses, ListScimDirectoryGroupsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim-bindings/{binding}/directory/groups',
    ...options
});

/**
 * Advertise what this SCIM service provider implements.
 *
 * The closed truth of what is implemented: no Bulk, no filter beyond the
 * four `eq` probes, no sorting, no ETag, no change password. Each absence
 * is advertised here AND refused by name at its endpoint, never
 * half-implemented.
 *
 */
export const scimServiceProviderConfig = <ThrowOnError extends boolean = false>(options: Options<ScimServiceProviderConfigData, ThrowOnError>) => (options.client ?? client).get<ScimServiceProviderConfigResponses, ScimServiceProviderConfigErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/ServiceProviderConfig',
    ...options
});

/**
 * The resource types this service provider implements.
 *
 * The closed set — User and Group, and nothing else.
 */
export const scimResourceTypes = <ThrowOnError extends boolean = false>(options: Options<ScimResourceTypesData, ThrowOnError>) => (options.client ?? client).get<ScimResourceTypesResponses, ScimResourceTypesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/ResourceTypes',
    ...options
});

/**
 * The schemas this service provider implements.
 */
export const scimSchemas = <ThrowOnError extends boolean = false>(options: Options<ScimSchemasData, ThrowOnError>) => (options.client ?? client).get<ScimSchemasResponses, ScimSchemasErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Schemas',
    ...options
});

/**
 * List or filter provisioned users.
 *
 * The filter grammar is closed to `userName eq "…"` and
 * `externalId eq "…"`; anything else is `invalidFilter`. Paging is
 * RFC-shaped and 1-based, `count` is clamped to the server's bound, and
 * an out-of-range page returns an empty `Resources` with a truthful
 * `totalResults`. `sortBy` is refused with 501: sorting is advertised
 * absent and is not half-implemented here.
 *
 */
export const scimListUsers = <ThrowOnError extends boolean = false>(options: Options<ScimListUsersData, ThrowOnError>) => (options.client ?? client).get<ScimListUsersResponses, ScimListUsersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Users',
    ...options
});

/**
 * Provision a user, pre-linked and with zero grants.
 *
 * Creates the account with its external identity ALREADY BOUND — no
 * invitation token, no credential-establishment authority, no session, no
 * assurance. If the asserted identity already exists instance-wide the
 * account is ATTACHED instead, through the same query path and with a
 * byte-shape-identical response, so a caller cannot tell the two apart.
 * Grants come only from group mappings. The `password` attribute is
 * refused by name: provisioning never establishes credentials.
 *
 */
export const scimCreateUser = <ThrowOnError extends boolean = false>(options: Options<ScimCreateUserData, ThrowOnError>) => (options.client ?? client).post<ScimCreateUserResponses, ScimCreateUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Users',
    ...options,
    headers: {
        'Content-Type': 'application/scim+json',
        ...options.headers
    }
});

/**
 * Deprovision and remove a user from this binding's directory.
 *
 * Releases every origin this binding holds for the user, removes every
 * member reference to them from this binding's groups in the same
 * transaction, and advances their session generation UNCONDITIONALLY —
 * even when no grant row changed, because the identity provider has
 * declared this human gone. The account and its identity links survive:
 * other organisations' memberships and other providers are not this
 * provider's to kill.
 *
 */
export const scimDeleteUser = <ThrowOnError extends boolean = false>(options: Options<ScimDeleteUserData, ThrowOnError>) => (options.client ?? client).delete<ScimDeleteUserResponses, ScimDeleteUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Users/{id}',
    ...options
});

/**
 * Read one provisioned user.
 */
export const scimGetUser = <ThrowOnError extends boolean = false>(options: Options<ScimGetUserData, ThrowOnError>) => (options.client ?? client).get<ScimGetUserResponses, ScimGetUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Users/{id}',
    ...options
});

/**
 * Patch a provisioned user through the closed operation matrix.
 *
 * Accepted cells only — add/replace on the pathless value object, on a
 * plain attribute path and on `active`; remove on a non-required plain
 * attribute. Everything else is `invalidPath`, `members` on a User
 * included. A PATCH is ATOMIC: one invalid operation fails the whole
 * request with nothing committed. Stringified booleans for `active` are
 * normalized on ingest, a named tolerance.
 *
 */
export const scimPatchUser = <ThrowOnError extends boolean = false>(options: Options<ScimPatchUserData, ThrowOnError>) => (options.client ?? client).patch<ScimPatchUserResponses, ScimPatchUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Users/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/scim+json',
        ...options.headers
    }
});

/**
 * Replace a provisioned user (RFC replacement).
 *
 * Omitted mutable attributes clear to their defaults and `active`
 * defaults TRUE, so an omitted `active` REACTIVATES. `userName` is
 * required. The subject source is exempt from replacement: the subject is
 * write-once per resource, and a change is refused with `mutability` —
 * an identifier migration at the identity provider is the rebinding
 * hazard the identity model exists to prevent. `groups` is ignored on
 * input; membership is authored exclusively through Group operations.
 *
 */
export const scimReplaceUser = <ThrowOnError extends boolean = false>(options: Options<ScimReplaceUserData, ThrowOnError>) => (options.client ?? client).put<ScimReplaceUserResponses, ScimReplaceUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Users/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/scim+json',
        ...options.headers
    }
});

/**
 * List or filter provisioned groups.
 *
 * `displayName eq "…"` and `externalId eq "…"` only. The first is
 * load-bearing rather than a convenience: Okta and Entra both discover a
 * group by it before creating or updating one.
 *
 */
export const scimListGroups = <ThrowOnError extends boolean = false>(options: Options<ScimListGroupsData, ThrowOnError>) => (options.client ?? client).get<ScimListGroupsResponses, ScimListGroupsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Groups',
    ...options
});

/**
 * Provision a group.
 *
 * Members are references to THIS binding's provisioned users; one
 * resolving to no such user is refused with `invalidValue`, because the
 * identity provider can only reference ids this server minted.
 * Group-typed members are refused by name: v1 is flat.
 *
 */
export const scimCreateGroup = <ThrowOnError extends boolean = false>(options: Options<ScimCreateGroupData, ThrowOnError>) => (options.client ?? client).post<ScimCreateGroupResponses, ScimCreateGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Groups',
    ...options,
    headers: {
        'Content-Type': 'application/scim+json',
        ...options.headers
    }
});

/**
 * Delete a provisioned group.
 *
 * Releases that group's origins for every member and flips every
 * referencing mapping row to INERT with an attention state, rather than
 * deleting rows behind the administrator's back: a row the server tidied
 * away is a row nobody got to decide about.
 *
 */
export const scimDeleteGroup = <ThrowOnError extends boolean = false>(options: Options<ScimDeleteGroupData, ThrowOnError>) => (options.client ?? client).delete<ScimDeleteGroupResponses, ScimDeleteGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Groups/{id}',
    ...options
});

/**
 * Read one provisioned group.
 */
export const scimGetGroup = <ThrowOnError extends boolean = false>(options: Options<ScimGetGroupData, ThrowOnError>) => (options.client ?? client).get<ScimGetGroupResponses, ScimGetGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Groups/{id}',
    ...options
});

/**
 * Patch a provisioned group through the closed operation matrix.
 *
 * Accepted cells only — add/replace on the pathless value object, on a
 * plain attribute path and on `members`; remove on a non-required plain
 * attribute, on `members` (clear) and on `members[value eq "…"]` (remove
 * one reference). Everything else is `invalidPath`, `active` on a Group
 * included.
 *
 */
export const scimPatchGroup = <ThrowOnError extends boolean = false>(options: Options<ScimPatchGroupData, ThrowOnError>) => (options.client ?? client).patch<ScimPatchGroupResponses, ScimPatchGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Groups/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/scim+json',
        ...options.headers
    }
});

/**
 * Replace a provisioned group (RFC replacement).
 *
 * `displayName` is required and the member set is replaced wholesale.
 * Adding a member creates that user's origins from this group's mapping
 * rows; removing one releases exactly this group's origins for them.
 *
 */
export const scimReplaceGroup = <ThrowOnError extends boolean = false>(options: Options<ScimReplaceGroupData, ThrowOnError>) => (options.client ?? client).put<ScimReplaceGroupResponses, ScimReplaceGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Groups/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/scim+json',
        ...options.headers
    }
});

/**
 * Refused — Bulk is not implemented.
 *
 * Advertised absent in ServiceProviderConfig and refused HERE by name,
 * with the RFC 7644 error body and HTTP 501. It carries no `scimType`:
 * that field is a 400-class discriminator, and inventing one for a 501
 * would name a code the RFC does not define. The route exists so the
 * refusal is the protocol's own shape rather than a generic 404.
 *
 */
export const scimBulk = <ThrowOnError extends boolean = false>(options: Options<ScimBulkData, ThrowOnError>) => (options.client ?? client).post<unknown, ScimBulkErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Bulk',
    ...options
});

/**
 * Refused — /Me is not implemented.
 *
 * A provisioning connection has no "me": it asserts identities, it does
 * not have one. Advertised absent and refused by name with HTTP 501.
 *
 */
export const scimMe = <ThrowOnError extends boolean = false>(options: Options<ScimMeData, ThrowOnError>) => (options.client ?? client).get<unknown, ScimMeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Me',
    ...options
});

/**
 * Refused — the .search POST query is not implemented.
 *
 * The filter grammar this provider answers is closed to four `eq` probes
 * and is expressed as a query parameter. A POST query would be a second,
 * wider filter surface; advertised absent and refused by name with 501.
 *
 */
export const scimSearchUsers = <ThrowOnError extends boolean = false>(options: Options<ScimSearchUsersData, ThrowOnError>) => (options.client ?? client).post<unknown, ScimSearchUsersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Users/.search',
    ...options
});

/**
 * Refused — the .search POST query is not implemented.
 */
export const scimSearchGroups = <ThrowOnError extends boolean = false>(options: Options<ScimSearchGroupsData, ThrowOnError>) => (options.client ?? client).post<unknown, ScimSearchGroupsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/scim/v2/{binding}/Groups/.search',
    ...options
});

/**
 * This instance's directory listing, as served to a connected instance.
 *
 * The ONE endpoint an instance-connection credential may reach. Artifact
 * eligibility caps the credential to this operation alone, so a future
 * operation adopting the `instance-directory` formula does not widen any
 * existing token.
 *
 * What it returns, exhaustively: this instance's opaque identity, its
 * version, and the NAMES and counts of organisations and projects. No
 * values, no keys, no environments, no membership, no settings, no audit —
 * the response schema has no field that could carry one.
 *
 * The identity is returned ONLY here, never pre-authentication. That is
 * what makes self-connection detectable at the fetch rather than guessable
 * from outside.
 *
 * Human holders of `instance-directory` reach it too: it is the same
 * listing, and hiding it from the humans the atom exists for would be
 * arbitrary.
 *
 */
export const serveDirectory = <ThrowOnError extends boolean = false>(options?: Options<ServeDirectoryData, ThrowOnError>) => (options?.client ?? client).get<ServeDirectoryResponses, ServeDirectoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/directory',
    ...options
});

/**
 * The directory of connected instances.
 *
 * Fetched ON VIEW. There is no background poller: a fetch happens because
 * a holder of `instance-directory` is looking, and at no other moment.
 *
 * An unreachable entry serves its last-known snapshot MARKED STALE with
 * its age, never silently as current. A credential rejection is its own
 * loud state, distinct from unreachability, because the operator's fix
 * differs.
 *
 * When the instance-wide or per-viewer trigger budget is exhausted no
 * round runs and every entry is served from its snapshot, marked stale —
 * the bounds limit fetch triggers, not views.
 *
 */
export const listRemotes = <ThrowOnError extends boolean = false>(options?: Options<ListRemotesData, ThrowOnError>) => (options?.client ?? client).get<ListRemotesResponses, ListRemotesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/remotes',
    ...options
});

/**
 * Add a connection entry, verified by one authenticated fetch.
 *
 * URL AND PIN ARE IMMUTABLE once stored, so there is no edit verb
 * anywhere: re-pointing a stored credential at a different host is the
 * credential-redirect attack, and re-pointing is remove + add, which
 * re-runs the full ceremony including the human fingerprint confirmation.
 *
 * The caller must already have displayed the SPKI fingerprint and taken
 * the human's confirmation — that is interactive and belongs to the client.
 * This endpoint performs the authenticated directory fetch, which returns
 * the remote's instance identity: self-connection (409) and
 * duplicate-identity (409) fire there, BEFORE the entry is committed. A
 * fetch that does not succeed commits nothing (422).
 *
 * The credential is write-only after storage: sealed under the instance
 * keyring, never re-displayable through any surface, and leaving the
 * process only inside TLS to the pinned remote.
 *
 */
export const addRemote = <ThrowOnError extends boolean = false>(options: Options<AddRemoteData, ThrowOnError>) => (options.client ?? client).post<AddRemoteResponses, AddRemoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/remotes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Destroy an entry, its stored credential and its snapshot.
 *
 * THIS IS NOT REVOCATION. The serving side's credential remains valid
 * until it is revoked THERE, and every surface says so: destroying the
 * local copy is not revocation and this API does not pretend otherwise.
 *
 */
export const removeRemote = <ThrowOnError extends boolean = false>(options: Options<RemoveRemoteData, ThrowOnError>) => (options.client ?? client).delete<RemoveRemoteResponses, RemoveRemoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/remotes/{remote}',
    ...options
});

/**
 * One connection entry, full detail, fetched on view.
 */
export const showRemote = <ThrowOnError extends boolean = false>(options: Options<ShowRemoteData, ThrowOnError>) => (options.client ?? client).get<ShowRemoteResponses, ShowRemoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/remotes/{remote}',
    ...options
});

/**
 * Change an entry's display name.
 *
 * The display name is the ONE mutable field. There is no URL or pin edit
 * here or anywhere; see `addRemote` for why.
 *
 * API and UI only, deliberately: the API/CLI ADR's closed verb list for
 * this family is `remote add|list|show|remove`, and parity holds because
 * this is public surface.
 *
 */
export const renameRemote = <ThrowOnError extends boolean = false>(options: Options<RenameRemoteData, ThrowOnError>) => (options.client ?? client).patch<RenameRemoteResponses, RenameRemoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/remotes/{remote}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The connection credentials this instance has minted.
 *
 * Metadata only, never the value: a credential is display-once at mint
 * and write-only after, and no statement anywhere reads a verifier back
 * out for display.
 *
 */
export const listInstanceConnections = <ThrowOnError extends boolean = false>(options?: Options<ListInstanceConnectionsData, ThrowOnError>) => (options?.client ?? client).get<ListInstanceConnectionsResponses, ListInstanceConnectionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/connections',
    ...options
});

/**
 * Mint a connection principal and its one credential.
 *
 * Principal and credential are ONE UNIT with a stable immutable id, and
 * there is one credential per principal EVER — rotation is a new mint plus
 * a revoke of the predecessor, never a second credential on this one.
 *
 * The value is returned EXACTLY ONCE, here. It is not retrievable
 * afterwards through any surface.
 *
 * The label names the intended peer for the audit trail. It is
 * descriptive and NOT enforced: this instance cannot verify who holds the
 * token, and does not pretend to.
 *
 */
export const mintInstanceConnection = <ThrowOnError extends boolean = false>(options: Options<MintInstanceConnectionData, ThrowOnError>) => (options.client ?? client).post<MintInstanceConnectionResponses, MintInstanceConnectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/connections',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke the credential and retire its principal with it.
 *
 * It bites at the NEXT presentation: the liveness predicate is read in the
 * authenticating transaction, uncached, so there is no window in which a
 * revoked connection still serves a listing.
 *
 * A double revoke is a 409, not a silent success — an act that did not
 * happen must not put a second event on the trail.
 *
 */
export const revokeInstanceConnection = <ThrowOnError extends boolean = false>(options: Options<RevokeInstanceConnectionData, ThrowOnError>) => (options.client ?? client).delete<RevokeInstanceConnectionResponses, RevokeInstanceConnectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/connections/{connection}',
    ...options
});

/**
 * One connection's metadata.
 */
export const showInstanceConnection = <ThrowOnError extends boolean = false>(options: Options<ShowInstanceConnectionData, ThrowOnError>) => (options.client ?? client).get<ShowInstanceConnectionResponses, ShowInstanceConnectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/connections/{connection}',
    ...options
});

/**
 * De-allowlist an origin and kill every session bound to it.
 *
 * THE KILL SWITCH. Removing the entry and revoking every workspace session
 * bound to that origin happen in ONE transaction, so there is no window in
 * which the origin is de-allowlisted and its sessions still authenticate.
 * New handoffs refuse at the transaction, not merely at CORS.
 *
 * The origin travels in the REQUEST BODY and never as a path parameter,
 * deliberately: a path parameter naming an origin is one review lapse away
 * from a path parameter naming a target, which is the proxy shape this
 * contract refuses by construction.
 *
 */
export const removeWorkspaceOrigin = <ThrowOnError extends boolean = false>(options: Options<RemoveWorkspaceOriginData, ThrowOnError>) => (options.client ?? client).delete<RemoveWorkspaceOriginResponses, RemoveWorkspaceOriginErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/workspace-origins',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The origin allowlist — which UI origins may run the handoff.
 *
 * EXACT origins. No wildcards, no subdomain matching.
 *
 * It names UI ORIGINS, not instances, deliberately: what is trusted is
 * precisely the code served at that origin, and that is what an admin must
 * be able to point at when auditing why cross-origin access exists.
 *
 */
export const listWorkspaceOrigins = <ThrowOnError extends boolean = false>(options?: Options<ListWorkspaceOriginsData, ThrowOnError>) => (options?.client ?? client).get<ListWorkspaceOriginsResponses, ListWorkspaceOriginsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/workspace-origins',
    ...options
});

/**
 * Consent to one exact UI origin.
 */
export const addWorkspaceOrigin = <ThrowOnError extends boolean = false>(options: Options<AddWorkspaceOriginData, ThrowOnError>) => (options.client ?? client).post<AddWorkspaceOriginResponses, AddWorkspaceOriginErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/workspace-origins',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Open a workspace handoff transaction.
 *
 * The browser edition of the RFC 8252 shape the CLI already uses: the
 * front channel carries code and state ONLY, never the artifact.
 *
 * Pre-authentication and CORS-readable for an allowlisted origin. A
 * non-allowlisted origin is refused HERE, at the transaction — CORS
 * controls what a browser may read, never what a token may do.
 *
 * The viewing shell opens the returned authorization URL in a popup with
 * `noopener`, so the return path is not `window.opener`: the popup's final
 * redirect lands on a same-origin callback page of the viewing UI, which
 * hands the result to the shell over a nonce-named `BroadcastChannel`.
 *
 */
export const startWorkspaceHandoff = <ThrowOnError extends boolean = false>(options: Options<StartWorkspaceHandoffData, ThrowOnError>) => (options.client ?? client).post<StartWorkspaceHandoffResponses, StartWorkspaceHandoffErrors, ThrowOnError>({
    url: '/api/v1/auth/workspace/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Approve a handoff as the authenticated human, minting the code.
 *
 * Runs INSIDE THE POPUP, on this instance's own origin, under this
 * instance's own ceremonies — password + TOTP, passkey, OIDC, whatever
 * this instance's human-auth configuration says. Authentication to a
 * remote always happens on the remote's own origin; that is a design rule,
 * not a platform limitation.
 *
 * It takes a HUMAN SESSION and refuses a workspace bearer by construction:
 * a cross-origin credential living in another origin's JavaScript must not
 * be able to authorize the issuance of another one.
 *
 */
export const approveWorkspaceHandoff = <ThrowOnError extends boolean = false>(options: Options<ApproveWorkspaceHandoffData, ThrowOnError>) => (options.client ?? client).post<ApproveWorkspaceHandoffResponses, ApproveWorkspaceHandoffErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/workspace/approve',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Exchange code + PKCE verifier for a workspace session.
 *
 * Single-use and atomically consumed: two concurrent redemptions of one
 * code cannot both yield a session.
 *
 * The result is an opaque bearer whose server-side row here carries
 * everything the locked session model requires, plus its two bound extras:
 * the requesting origin and the issuing transaction. It differs from a
 * browser session ONLY in transport — presented in an `Authorization`
 * header, never a cookie — which is why CORS runs without credentials mode
 * and this instance's CSRF posture is untouched.
 *
 * CLIENT-SIDE THE BEARER LIVES IN JS MEMORY ONLY: never a cookie, never
 * localStorage or sessionStorage, gone on tab close.
 *
 */
export const redeemWorkspaceHandoff = <ThrowOnError extends boolean = false>(options: Options<RedeemWorkspaceHandoffData, ThrowOnError>) => (options.client ?? client).post<RedeemWorkspaceHandoffResponses, RedeemWorkspaceHandoffErrors, ThrowOnError>({
    url: '/api/v1/auth/workspace/redeem',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Load the authoritative shape of a live workspace handoff.
 *
 * The approve page reads the operation, environment and enumerated key set
 * the transaction was opened against, rather than trusting them from its
 * own URL — the key set can be large, and a URL is a poor and length-capped
 * place for the authoritative binding to live. Only the server-bound
 * transaction answers, and only to the authenticated human on this
 * instance; the numbers here are identifiers, never values or verifiers.
 *
 */
export const showWorkspaceHandoff = <ThrowOnError extends boolean = false>(options: Options<ShowWorkspaceHandoffData, ThrowOnError>) => (options.client ?? client).get<ShowWorkspaceHandoffResponses, ShowWorkspaceHandoffErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/workspace/transactions/{state}',
    ...options
});

/**
 * The caller's own active sessions, workspace sessions included.
 *
 * SELF-SCOPED: the caller's sessions and nothing else. It needs no
 * capability for the reason `listMyOrgs` needs none — it is a projection
 * of what the caller already holds — and requiring one would make incident
 * response depend on an authority an attacker may have just removed.
 *
 * A WORKSPACE SESSION APPEARS HERE AS ITS OWN ARTIFACT TYPE, carrying the
 * requesting origin. That is the ADR's requirement and the reason a
 * workspace session is a session row at all.
 *
 * Metadata only. No verifier is returned here or anywhere.
 *
 */
export const listMySessions = <ThrowOnError extends boolean = false>(options?: Options<ListMySessionsData, ThrowOnError>) => (options?.client ?? client).get<ListMySessionsResponses, ListMySessionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me/sessions',
    ...options
});

/**
 * Revoke one of the caller's own sessions.
 *
 * Kills it MID-FLIGHT: the next request re-resolves the row in its own
 * transaction, so a revoked session stops working immediately rather than
 * at some cache horizon.
 *
 * The principal conjunct lives in the SQL, so one caller is structurally
 * unable to reach another's session by guessing an id — and an id that is
 * not the caller's is a 404, not a 403.
 *
 */
export const revokeMySession = <ThrowOnError extends boolean = false>(options: Options<RevokeMySessionData, ThrowOnError>) => (options.client ?? client).delete<RevokeMySessionResponses, RevokeMySessionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me/sessions/{session}',
    ...options
});

/**
 * Publish a selection of your own pending changes.
 *
 * SELECTIVE. The body names version ids, and only those - plus whatever
 * key-group closure pulls in - are committed. Every other pending change,
 * the caller's own unselected ones and every other principal's, is
 * invisible to the computation, so the materialized snapshot always
 * corresponds to a state the publisher previewed.
 *
 * A publish carries ONLY the caller's own pending changes. There is no
 * cross-user hand-off of staged work: a key group whose members carry a
 * pending change owned by another principal is refused, loud, naming the
 * group and the key, never silently split.
 *
 * The whole operation is one serializable transaction holding the
 * per-project lock. A version whose published cell has moved since it was
 * staged is refused loud rather than rebased, and a `required_in` key
 * left absent vetoes the publish naming key and environment.
 *
 * Publishing a version whose environment differs from the addressed one
 * is permitted and each such environment is authorized for `publish`
 * separately, immediately before commit.
 *
 * Every protected environment reached requires its bound publish
 * confirmation. Machine callers name the exact protected set they
 * reviewed; human callers consume the bound ceremony. A selection containing restore-authored drafts also
 * requires the preview token returned by rollback; schema, published
 * state, authorization, selection, or key-group closure drift invalidates
 * that token and requires a fresh rollback preview.
 *
 */
export const publishPendingChanges = <ThrowOnError extends boolean = false>(options: Options<PublishPendingChangesData, ThrowOnError>) => (options.client ?? client).post<PublishPendingChangesResponses, PublishPendingChangesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/publish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The project's change-approval policies.
 *
 * Secret-change approvals (#151). The scoped policies governing publishes
 * in this project: their approvers (users and SCIM groups), quorum,
 * self-approval behaviour, request expiry and emergency bypassers. Reading
 * the set is an administrative act and is itself audited.
 *
 */
export const listApprovalPolicies = <ThrowOnError extends boolean = false>(options: Options<ListApprovalPoliciesData, ThrowOnError>) => (options.client ?? client).get<ListApprovalPoliciesResponses, ListApprovalPoliciesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/approval-policies',
    ...options
});

/**
 * Create a change-approval policy.
 *
 * Bind an approval policy to the project or one of its environments
 * (`environment_id` empty means every environment). Staging stays
 * unrestricted; a covered PUBLISH then requires the policy's completed
 * approval state.
 *
 */
export const createApprovalPolicy = <ThrowOnError extends boolean = false>(options: Options<CreateApprovalPolicyData, ThrowOnError>) => (options.client ?? client).post<CreateApprovalPolicyResponses, CreateApprovalPolicyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/approval-policies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a change-approval policy.
 *
 * Remove the policy and its member sets. Open requests pinned to it fail
 * closed at merge; the audit trail of resolved requests is retained.
 *
 */
export const deleteApprovalPolicy = <ThrowOnError extends boolean = false>(options: Options<DeleteApprovalPolicyData, ThrowOnError>) => (options.client ?? client).delete<DeleteApprovalPolicyResponses, DeleteApprovalPolicyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/approval-policies/{policy}',
    ...options
});

/**
 * Update a change-approval policy.
 *
 * Replace the policy's fields and member sets. The version increments, so
 * every open request pinned to the previous version fails closed the next
 * time it is voted on or merged. Disabling is `enabled: false`.
 *
 */
export const updateApprovalPolicy = <ThrowOnError extends boolean = false>(options: Options<UpdateApprovalPolicyData, ThrowOnError>) => (options.client ?? client).put<UpdateApprovalPolicyResponses, UpdateApprovalPolicyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/approval-policies/{policy}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The environment's change-approval requests.
 *
 * The review queue for one environment: each request's pinned change set
 * size, purpose, state, quorum progress and votes. No value plaintext is
 * carried. Gated by `read` on the environment.
 *
 */
export const listApprovalRequests = <ThrowOnError extends boolean = false>(options: Options<ListApprovalRequestsData, ThrowOnError>) => (options.client ?? client).get<ListApprovalRequestsResponses, ListApprovalRequestsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/approval-requests',
    ...options
});

/**
 * Approve or reject a change-approval request.
 *
 * An eligible approver casts one decision. A repeated identical decision
 * is idempotent; a conflicting one is refused. A reject resolves the
 * request; approvals accumulate toward the policy's quorum. The requester
 * cannot approve their own request unless the policy permits it. The vote
 * consumes a purpose-bound reauthentication ceremony.
 *
 */
export const voteApprovalRequest = <ThrowOnError extends boolean = false>(options: Options<VoteApprovalRequestData, ThrowOnError>) => (options.client ?? client).post<VoteApprovalRequestResponses, VoteApprovalRequestErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/approval-requests/{approvalRequest}/vote',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Prepare a purpose-bound approval ceremony.
 *
 * Returns the request's exact key unit and the acting session's current
 * reauthentication-window state. It carries no value material and is
 * authorized by `publish`, so a named approver does not need `read` or
 * `reveal` merely to prepare an otherwise-authorized decision.
 *
 */
export const getApprovalCeremony = <ThrowOnError extends boolean = false>(options: Options<GetApprovalCeremonyData, ThrowOnError>) => (options.client ?? client).get<GetApprovalCeremonyResponses, GetApprovalCeremonyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/approval-requests/{approvalRequest}/ceremony',
    ...options
});

/**
 * The matrix signals for one environment.
 *
 * The two "changed" signals the matrix renders, plus the environment's
 * current revision. `pending_version_id` is the caller's OWN live draft
 * for the cell - the id a selective publish names; `pending_by_others`
 * is the quieter collision marker and carries no id, no owner and no
 * operation, because another principal's draft is write-presence and
 * nothing more.
 *
 * Both signals degrade to write-presence for `secret` keys: they report
 * that someone WROTE here, never that the plaintext differs from the
 * published one. A comparison status is itself an oracle.
 *
 * This is also the advisory stream's documented POLLING FALLBACK: the
 * same facts, pulled under the caller's own authorization.
 *
 */
export const getEnvironmentSignals = <ThrowOnError extends boolean = false>(options: Options<GetEnvironmentSignalsData, ThrowOnError>) => (options.client ?? client).get<GetEnvironmentSignalsResponses, GetEnvironmentSignalsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/signals',
    ...options
});

/**
 * Preview the caller's pending drafts in one environment.
 *
 * Returns only the caller's own drafts. Other principals' drafts are
 * invisible here, including their presence; the matrix signals endpoint
 * carries the permitted write-presence marker instead.
 *
 * A config `set` carries its plaintext under read authorization unless
 * its staged material originated as secret. Secret-origin drafts never
 * carry material here and remain hidden: offering a
 * secret preview would require consuming a disclosure ceremony, which is
 * outside this seam's scope. Unset drafts carry no material. `value` is
 * present if and only if `revealed` is true.
 *
 */
export const listPendingDrafts = <ThrowOnError extends boolean = false>(options: Options<ListPendingDraftsData, ThrowOnError>) => (options.client ?? client).get<ListPendingDraftsResponses, ListPendingDraftsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/pending',
    ...options
});

/**
 * Stage an environment or key restore from one revision.
 *
 * Computes a two-way set/unset diff and writes ordinary caller-owned
 * pending changes. The normal publish verb commits them and validates
 * against the current schema. Reading superseded secret material also
 * requires read AND reveal-history; comparing current secret plaintext
 * also requires read AND reveal. Both take reauthentication and one
 * disclosure event per key and revision at this staging step. The response includes the full,
 * reveal-safe impact preview and its exact-input publish token.
 *
 */
export const rollbackRevision = <ThrowOnError extends boolean = false>(options: Options<RollbackRevisionData, ThrowOnError>) => (options.client ?? client).post<RollbackRevisionResponses, RollbackRevisionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/revisions/{revision}/rollback',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List durable workload revision pins.
 */
export const listRevisionPins = <ThrowOnError extends boolean = false>(options: Options<ListRevisionPinsData, ThrowOnError>) => (options.client ?? client).get<ListRevisionPinsResponses, ListRevisionPinsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/pins',
    ...options
});

/**
 * Create, reassign, or renew a workload revision pin.
 *
 * One durable pin may exist per workload and environment. Repeating this
 * operation renews the same revision or reassigns a different one, with
 * authorization rechecked. Omitted expiry defaults to 180 days; the
 * maximum is 365 days. A non-current revision requires reveal-history,
 * reauthentication, and one disclosure event per historical secret key;
 * a current-schema failure requires override_schema.
 *
 */
export const createRevisionPin = <ThrowOnError extends boolean = false>(options: Options<CreateRevisionPinData, ThrowOnError>) => (options.client ?? client).post<CreateRevisionPinResponses, CreateRevisionPinErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/pins',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Release a workload revision pin.
 *
 * Returns the released revision payload's retention consequence from the
 * same locked transaction that removes the pin. This is transaction-time
 * truth, not a durable retention promise: collection_eligible may be
 * collected immediately after commit, while concurrent GC that commits
 * first is reported as already_collected.
 *
 */
export const releaseRevisionPin = <ThrowOnError extends boolean = false>(options: Options<ReleaseRevisionPinData, ThrowOnError>) => (options.client ?? client).delete<ReleaseRevisionPinResponses, ReleaseRevisionPinErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/pins/{workloadPrincipal}',
    ...options
});

/**
 * One environment's revision history, newest first.
 *
 * LINEAGE ONLY: revision numbers, publishers, timestamps, the pinned
 * schema revision, and which keys changed. It never contains a value in
 * any form - not a plaintext, not a length, not a digest, not a
 * changed-from marker. That is what lets lineage be retained forever
 * while payloads are collected by retention policy.
 *
 */
export const listRevisions = <ThrowOnError extends boolean = false>(options: Options<ListRevisionsData, ThrowOnError>) => (options.client ?? client).get<ListRevisionsResponses, ListRevisionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/revisions',
    ...options
});

/**
 * One revision, with its change token.
 *
 * The change token is DERIVED here from the current root token key over
 * the snapshot's delivery manifest; it is never stored. That is what
 * makes `rotate-token-key` move every token while content, revision
 * numbers and pinned input revisions stay exactly where they are.
 *
 * The token is non-secret metadata by construction - keyed, so
 * unforgeable and un-invertible without the key, which is why it may flow
 * into pod annotations and logs. The key set it accompanies carries names
 * and classifications and no values.
 *
 */
export const getRevision = <ThrowOnError extends boolean = false>(options: Options<GetRevisionData, ThrowOnError>) => (options.client ?? client).get<GetRevisionResponses, GetRevisionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/revisions/{revision}',
    ...options
});

/**
 * Export one environment's resolved snapshot.
 *
 * The one BULK-DISCLOSURE verb, and what "fetch resolved" actually is. It
 * reads a committed snapshot, never live values.
 *
 * The formulas are stated separately because the capabilities imply
 * nothing about each other: current material is `read` AND `reveal`;
 * historical material - any revision that is not the latest - is `read`
 * AND `reveal-history`. Without `reveal`, `config` values are returned in
 * plaintext (classification IS the sensitivity boundary) and `secret`
 * keys report write-presence with no value.
 *
 * It is a POST because disclosure is an ACT: one audit event is written
 * per disclosed key, durably, before any plaintext leaves the server.
 *
 */
export const exportValues = <ThrowOnError extends boolean = false>(options: Options<ExportValuesData, ThrowOnError>) => (options.client ?? client).post<ExportValuesResponses, ExportValuesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/values/export',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The advisory live-update stream (SSE).
 *
 * Server-sent events, metadata only. An event says that something
 * changed - an environment advanced (`revision.published`), a principal
 * staged a draft (`pending.staged`), a published cell moved
 * (`cell.changed`) - and never what it changed to. It carries no value
 * in any form and NO CHANGE TOKEN.
 *
 * `cell.changed` claims exactly what the publish knew: that cell
 * advanced. A subscriber holding a draft on the named cell derives "my
 * draft is stale" from its own draft plus this event; the server never
 * asserts staleness it did not check.
 *
 * Every event is authorized against the environment it names, at emit
 * time, so a grant revoked mid-session takes effect on the NEXT event
 * rather than at the next reconnect. Events referencing objects the
 * recipient may not see are dropped, and an event reduced to nothing is
 * not sent.
 *
 * ADVISORY ONLY: correctness never depends on delivery. `Last-Event-ID`
 * replays nothing; a reconnecting client refetches current state from the
 * signals endpoint, which is the documented polling fallback. A client
 * that cannot keep up is disconnected rather than buffered. Connection
 * admission is bounded per principal, per organization, and instance-wide;
 * a refusal is 429. Accepted streams begin with a jittered `retry:` hint.
 *
 */
export const watchProjectEvents = <ThrowOnError extends boolean = false>(options: Options<WatchProjectEventsData, ThrowOnError, WatchProjectEventsResponse>) => (options.client ?? client).sse.get<WatchProjectEventsResponses, WatchProjectEventsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/events',
    ...options
});

/**
 * One bounded page of the organization's audit trail.
 *
 * Reads one bounded page of the append-only trail in stable
 * commit-sequence order, oldest first. Filtering by principal,
 * operation, outcome and resource identifiers is applied to the page
 * after it is read, so a page SCANS `limit` rows and RETURNS the subset
 * that matched: `next_after_seq` is the seq of the last row scanned (the
 * resume cursor), and `exhausted` is true only when the scan reached the
 * end of the trail. A sparse filter therefore returns few or zero items
 * with `exhausted` false — that means keep paging, not "no such events".
 *
 * Reading the trail is itself audited, unconditionally: each page commits
 * one `audit.query` event recording the normalized filter and the count
 * returned, durably, in the same transaction as the read. No value
 * material is ever in the trail, so none can be in this response.
 *
 */
export const queryOrgAudit = <ThrowOnError extends boolean = false>(options: Options<QueryOrgAuditData, ThrowOnError>) => (options.client ?? client).get<QueryOrgAuditResponses, QueryOrgAuditErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/audit',
    ...options
});

/**
 * Stream the organization's audit trail as JSONL.
 *
 * Streams the filtered slice of the trail as newline-delimited JSON, one
 * schema-versioned event per line, in gap-free commit-sequence order up
 * to a server-fixed snapshot taken before the first byte. The stream is
 * bounded on both ends: the server reads the trail one bounded page at a
 * time under a freshly minted proof, and the client writes bytes to disk
 * as they arrive rather than accumulating the history in memory.
 *
 * The export is itself an audited act: an `audit.export_started` INTENT
 * event is durable before the first byte and an `audit.export_completed`
 * OUTCOME event terminates the stream on success, page failure or client
 * disconnect. Authorization lost mid-stream stops it at the next page
 * boundary — a truncated download whose cause is durable in the trail. No
 * value material is ever in the trail, so none can be in the stream.
 *
 */
export const exportOrgAudit = <ThrowOnError extends boolean = false>(options: Options<ExportOrgAuditData, ThrowOnError>) => (options.client ?? client).get<ExportOrgAuditResponses, ExportOrgAuditErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/audit/export',
    ...options
});

/**
 * One bounded page of the project's audit trail.
 *
 * The project-scoped audit page. Same shape and paging contract as
 * `queryOrgAudit`, refined to the project chain.
 *
 */
export const queryProjectAudit = <ThrowOnError extends boolean = false>(options: Options<QueryProjectAuditData, ThrowOnError>) => (options.client ?? client).get<QueryProjectAuditResponses, QueryProjectAuditErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/audit',
    ...options
});

/**
 * Stream the project's audit trail as JSONL.
 *
 * The project-scoped JSONL export. Same shape and INTENT/OUTCOME
 * contract as `exportOrgAudit`, refined to the project chain.
 *
 */
export const exportProjectAudit = <ThrowOnError extends boolean = false>(options: Options<ExportProjectAuditData, ThrowOnError>) => (options.client ?? client).get<ExportProjectAuditResponses, ExportProjectAuditErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/audit/export',
    ...options
});

/**
 * One bounded page of the environment's audit trail.
 *
 * The environment-scoped audit page. Same shape and paging contract as
 * `queryOrgAudit`, refined to the environment chain.
 *
 */
export const queryEnvAudit = <ThrowOnError extends boolean = false>(options: Options<QueryEnvAuditData, ThrowOnError>) => (options.client ?? client).get<QueryEnvAuditResponses, QueryEnvAuditErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/audit',
    ...options
});

/**
 * Stream the environment's audit trail as JSONL.
 *
 * The environment-scoped JSONL export. Same shape and INTENT/OUTCOME
 * contract as `exportOrgAudit`, refined to the environment chain.
 *
 */
export const exportEnvAudit = <ThrowOnError extends boolean = false>(options: Options<ExportEnvAuditData, ThrowOnError>) => (options.client ?? client).get<ExportEnvAuditResponses, ExportEnvAuditErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/audit/export',
    ...options
});

/**
 * Rotate the root change-token key.
 *
 * Mints a new root token key, retires the old one, and derives every
 * subsequent change token under it.
 *
 * WHAT IT DOES NOT TOUCH: no snapshot's content, no revision number, and
 * no pinned input revision moves. It cannot - a change token is never
 * stored, it is derived at read from the live key over the snapshot's
 * delivery manifest, so there is no cache to invalidate and nothing to
 * resume after a crash.
 *
 * Every outstanding delivery cursor mismatches once and every consumer
 * performs one full fetch. This is NOT a restart wave.
 *
 * It rides `rotate-dek`: the capability set is closed and names four
 * rotation atoms for five rotation verbs, and the root token key is a
 * tier-3 key alongside the DEKs.
 *
 */
export const rotateTokenKey = <ThrowOnError extends boolean = false>(options?: Options<RotateTokenKeyData, ThrowOnError>) => (options?.client ?? client).post<RotateTokenKeyResponses, RotateTokenKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/rotate-token-key',
    ...options
});

/**
 * Append a new DEK version for one project or the instance scope.
 *
 * Appends a new data-encryption-key version. New writes seal under it
 * immediately; ciphertext under the previous version stays readable until
 * `reencrypt` walks it onto the new one (Vault keyring semantics). Free on
 * its own, and incomplete without a following reencrypt.
 *
 * Its own capability, `rotate-dek`, never bundled - the post-compromise
 * recovery order runs each rotation under a distinct authority.
 *
 */
export const rotateDek = <ThrowOnError extends boolean = false>(options: Options<RotateDekData, ThrowOnError>) => (options.client ?? client).post<RotateDekResponses, RotateDekErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/rotate-dek',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Walk the instance credential ciphertext onto the active DEK version.
 *
 * Moves every instance-DEK credential ciphertext (password verifiers, TOTP
 * seeds, recovery codes, OIDC/SAML secrets, remote credentials) onto the
 * active instance DEK version and retires the superseded versions — the
 * completion of an instance `rotate-dek`. Chunked and resumable; re-run to
 * continue after an interruption.
 *
 */
export const reencryptInstance = <ThrowOnError extends boolean = false>(options?: Options<ReencryptInstanceData, ThrowOnError>) => (options?.client ?? client).post<ReencryptInstanceResponses, ReencryptInstanceErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/reencrypt',
    ...options
});

/**
 * Generate a new master key and re-wrap every tier-3 key under it.
 *
 * Generates a new master key, re-wraps every tier-3 key (all project DEKs,
 * the instance DEK, the root token key) under it, and retires the old
 * master after a fenced zero-reference check. Bounded by the number of
 * projects, so seconds.
 *
 * Step 2 of the post-compromise recovery order: an attacker who held
 * process memory holds the master, so every DEK must be re-wrapped under a
 * master they never saw. Refused while the root is dual-wrapped — finalize
 * the root rotation first.
 *
 */
export const rotateMasterKey = <ThrowOnError extends boolean = false>(options?: Options<RotateMasterKeyData, ThrowOnError>) => (options?.client ?? client).post<RotateMasterKeyResponses, RotateMasterKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/rotate-master-key',
    ...options
});

/**
 * Run one phase of the crash-safe root-key rotation.
 *
 * Replaces the operator-held root through a three-phase protocol over a
 * dual-wrapped master. NO key material crosses the wire: `prepare` reads
 * the NEW root from the server-side source (HIKYO_NEW_ROOT_KEY_FILE) and
 * seals a second master wrapper; the operator then installs the new root
 * at the PRIMARY source; `verify` re-reads the primary source and confirms
 * it now unwraps the new wrapper; `finalize` retires the old wrapper.
 *
 * A crash at any point leaves the instance bootable under either root, and
 * every start warns until finalize. This is the rotation operators need
 * after an env-file leak — it re-wraps the master only, touching no data.
 *
 */
export const rotateRootKey = <ThrowOnError extends boolean = false>(options: Options<RotateRootKeyData, ThrowOnError>) => (options.client ?? client).post<RotateRootKeyResponses, RotateRootKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/rotate-root-key',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Walk a project's ciphertext onto the active DEK version.
 *
 * Moves every ciphertext in the project (values, drafts, snapshot payloads,
 * adapter credentials) onto the active project DEK version and retires the
 * superseded versions — the completion of a project `rotate-dek`. Chunked
 * and resumable.
 *
 */
export const reencryptProject = <ThrowOnError extends boolean = false>(options: Options<ReencryptProjectData, ThrowOnError>) => (options.client ?? client).post<ReencryptProjectResponses, ReencryptProjectErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/reencrypt',
    ...options
});

/**
 * Rotate the secret-scanning fingerprint key.
 *
 * Mints a new scanning-fingerprint key, retires the old one, and drops
 * every dismissal row in the same transaction.
 *
 * Outright replacement: there is no version keyring and no reencrypt walk,
 * because a fingerprint is a keyed digest, not a ciphertext to preserve.
 * Every stored fingerprint becomes unrecomputable under the new key, so
 * keeping the dismissals would silently suppress warns that must now
 * re-fire - dropping them is the safe direction.
 *
 * It rides `rotate-dek`, the same tier-3 rotation authority as
 * `rotate-token-key`.
 *
 */
export const rotateScanningKey = <ThrowOnError extends boolean = false>(options?: Options<RotateScanningKeyData, ThrowOnError>) => (options?.client ?? client).post<RotateScanningKeyResponses, RotateScanningKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/instance/rotate-scanning-key',
    ...options
});

/**
 * List project deployment adapters with redacted credential metadata.
 */
export const listAdapters = <ThrowOnError extends boolean = false>(options: Options<ListAdaptersData, ThrowOnError>) => (options.client ?? client).get<ListAdaptersResponses, ListAdaptersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapters',
    ...options
});

/**
 * Atomically create a Forgejo adapter, credential, and first valid target.
 */
export const createAdapter = <ThrowOnError extends boolean = false>(options: Options<CreateAdapterData, ThrowOnError>) => (options.client ?? client).post<CreateAdapterResponses, CreateAdapterErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapters',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Tombstone an adapter and scrub its remote names by default.
 */
export const deleteAdapter = <ThrowOnError extends boolean = false>(options: Options<DeleteAdapterData, ThrowOnError>) => (options.client ?? client).delete<DeleteAdapterResponses, DeleteAdapterErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapters/{adapter}',
    ...options
});

/**
 * Show one adapter and its targets; the credential is always redacted.
 */
export const showAdapter = <ThrowOnError extends boolean = false>(options: Options<ShowAdapterData, ThrowOnError>) => (options.client ?? client).get<ShowAdapterResponses, ShowAdapterErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapters/{adapter}',
    ...options
});

/**
 * Replace the adapter origin after its old remote custody is resolved.
 */
export const updateAdapterOrigin = <ThrowOnError extends boolean = false>(options: Options<UpdateAdapterOriginData, ThrowOnError>) => (options.client ?? client).patch<UpdateAdapterOriginResponses, UpdateAdapterOriginErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapters/{adapter}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cancel an attention-required move and reconverge the old route.
 */
export const cancelAdapterMove = <ThrowOnError extends boolean = false>(options: Options<CancelAdapterMoveData, ThrowOnError>) => (options.client ?? client).delete<CancelAdapterMoveResponses, CancelAdapterMoveErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-moves/{move}',
    ...options
});

/**
 * Poll a durable adapter route move without exposing either credential.
 */
export const showAdapterMove = <ThrowOnError extends boolean = false>(options: Options<ShowAdapterMoveData, ThrowOnError>) => (options.client ?? client).get<ShowAdapterMoveResponses, ShowAdapterMoveErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-moves/{move}',
    ...options
});

/**
 * Replace an attention-required pending route and resume activation.
 */
export const resumeAdapterMove = <ThrowOnError extends boolean = false>(options: Options<ResumeAdapterMoveData, ThrowOnError>) => (options.client ?? client).patch<ResumeAdapterMoveResponses, ResumeAdapterMoveErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-moves/{move}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Destroy outbound credential custody; remote scrub may then be impossible.
 */
export const revokeAdapterCredential = <ThrowOnError extends boolean = false>(options: Options<RevokeAdapterCredentialData, ThrowOnError>) => (options.client ?? client).delete<RevokeAdapterCredentialResponses, RevokeAdapterCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapters/{adapter}/credential',
    ...options
});

/**
 * Replace the write-only Forgejo token.
 */
export const setAdapterCredential = <ThrowOnError extends boolean = false>(options: Options<SetAdapterCredentialData, ThrowOnError>) => (options.client ?? client).put<SetAdapterCredentialResponses, SetAdapterCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapters/{adapter}/credential',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List targets nested under an adapter.
 */
export const listAdapterTargets = <ThrowOnError extends boolean = false>(options: Options<ListAdapterTargetsData, ThrowOnError>) => (options.client ?? client).get<ListAdapterTargetsResponses, ListAdapterTargetsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapters/{adapter}/targets',
    ...options
});

/**
 * Add a tested target and atomically reassign adapter authority.
 */
export const addAdapterTarget = <ThrowOnError extends boolean = false>(options: Options<AddAdapterTargetData, ThrowOnError>) => (options.client ?? client).post<AddAdapterTargetResponses, AddAdapterTargetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapters/{adapter}/targets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Tombstone a target and scrub its remote names by default.
 */
export const removeAdapterTarget = <ThrowOnError extends boolean = false>(options: Options<RemoveAdapterTargetData, ThrowOnError>) => (options.client ?? client).delete<RemoveAdapterTargetResponses, RemoveAdapterTargetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-targets/{target}',
    ...options
});

/**
 * Show target state, conflicts, mapping, or canonical workflow mapping.
 */
export const showAdapterTarget = <ThrowOnError extends boolean = false>(options: Options<ShowAdapterTargetData, ThrowOnError>) => (options.client ?? client).get<ShowAdapterTargetResponses, ShowAdapterTargetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-targets/{target}',
    ...options
});

/**
 * Replace target routing fields and the complete key subset.
 */
export const updateAdapterTarget = <ThrowOnError extends boolean = false>(options: Options<UpdateAdapterTargetData, ThrowOnError>) => (options.client ?? client).patch<UpdateAdapterTargetResponses, UpdateAdapterTargetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-targets/{target}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Produce a value-blind name plan and a conflict artifact when needed.
 */
export const planAdapterTarget = <ThrowOnError extends boolean = false>(options: Options<PlanAdapterTargetData, ThrowOnError>) => (options.client ?? client).post<PlanAdapterTargetResponses, PlanAdapterTargetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-targets/{target}/plan',
    ...options
});

/**
 * Enqueue a newest-wins manual converge.
 */
export const syncAdapterTarget = <ThrowOnError extends boolean = false>(options: Options<SyncAdapterTargetData, ThrowOnError>) => (options.client ?? client).post<SyncAdapterTargetResponses, SyncAdapterTargetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-targets/{target}/sync',
    ...options
});

/**
 * Stop every push for the target without releasing owned destination state.
 *
 * Pause supersedes the target's active job and fences any worker still
 * inside it; the ownership ledger is retained, so resume needs no
 * re-adoption. Publishes skip a paused target and a manual sync is
 * refused with 409. Pause narrows what Hikyo does at the destination, so
 * it carries `manage-adapters` alone and no reauthentication ceremony.
 * Idempotent.
 *
 */
export const pauseAdapterTarget = <ThrowOnError extends boolean = false>(options: Options<PauseAdapterTargetData, ThrowOnError>) => (options.client ?? client).post<PauseAdapterTargetResponses, PauseAdapterTargetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-targets/{target}/pause',
    ...options
});

/**
 * Resume a paused target and queue one catch-up converge.
 *
 * Resume pushes again, so it carries the manual-sync formula and the
 * adapter reauthentication ceremony over the adapter's environment set.
 * The response names the published revision the queued converge will
 * reach. A target that is not paused is refused with 409.
 *
 */
export const resumeAdapterTarget = <ThrowOnError extends boolean = false>(options: Options<ResumeAdapterTargetData, ThrowOnError>) => (options.client ?? client).post<ResumeAdapterTargetResponses, ResumeAdapterTargetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-targets/{target}/resume',
    ...options
});

/**
 * Probe Forgejo version and destination identity without values or writes.
 */
export const testAdapterTarget = <ThrowOnError extends boolean = false>(options: Options<TestAdapterTargetData, ThrowOnError>) => (options.client ?? client).post<TestAdapterTargetResponses, TestAdapterTargetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-targets/{target}/test',
    ...options
});

/**
 * Adopt an exact enumerated conflict artifact and enqueue convergence.
 */
export const adoptAdapterTargetNames = <ThrowOnError extends boolean = false>(options: Options<AdoptAdapterTargetNamesData, ThrowOnError>) => (options.client ?? client).post<AdoptAdapterTargetNamesResponses, AdoptAdapterTargetNamesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/adapter-targets/{target}/adoptions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List dynamic-secret providers with redacted credential metadata.
 */
export const listDynamicProviders = <ThrowOnError extends boolean = false>(options: Options<ListDynamicProvidersData, ThrowOnError>) => (options.client ?? client).get<ListDynamicProvidersResponses, ListDynamicProvidersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/dynamic-providers',
    ...options
});

/**
 * Configure a PostgreSQL provider; the admin credential is write-only.
 */
export const createDynamicProvider = <ThrowOnError extends boolean = false>(options: Options<CreateDynamicProviderData, ThrowOnError>) => (options.client ?? client).post<CreateDynamicProviderResponses, CreateDynamicProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/dynamic-providers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a provider; refuses while active leases exist unless revoke_all.
 */
export const deleteDynamicProvider = <ThrowOnError extends boolean = false>(options: Options<DeleteDynamicProviderData, ThrowOnError>) => (options.client ?? client).delete<DeleteDynamicProviderResponses, DeleteDynamicProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/dynamic-providers/{provider}',
    ...options
});

/**
 * Show one provider's status and metadata.
 */
export const showDynamicProvider = <ThrowOnError extends boolean = false>(options: Options<ShowDynamicProviderData, ThrowOnError>) => (options.client ?? client).get<ShowDynamicProviderResponses, ShowDynamicProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/dynamic-providers/{provider}',
    ...options
});

/**
 * Destroy admin custody; renewal and revocation may then be impossible.
 */
export const revokeDynamicProviderCredential = <ThrowOnError extends boolean = false>(options: Options<RevokeDynamicProviderCredentialData, ThrowOnError>) => (options.client ?? client).delete<RevokeDynamicProviderCredentialResponses, RevokeDynamicProviderCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/dynamic-providers/{provider}/credential',
    ...options
});

/**
 * Replace the write-only admin credential.
 */
export const setDynamicProviderCredential = <ThrowOnError extends boolean = false>(options: Options<SetDynamicProviderCredentialData, ThrowOnError>) => (options.client ?? client).put<SetDynamicProviderCredentialResponses, SetDynamicProviderCredentialErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/dynamic-providers/{provider}/credential',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List an environment's leases with status and metadata, never the secret.
 */
export const listLeases = <ThrowOnError extends boolean = false>(options: Options<ListLeasesData, ThrowOnError>) => (options.client ?? client).get<ListLeasesResponses, ListLeasesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/leases',
    ...options
});

/**
 * Mint a short-lived credential; the secret is disclosed exactly once.
 *
 * Display-once: the password is in this response and nowhere else, ever.
 * A retry mints a NEW lease with a NEW secret; the old secret is never
 * returned again. A machine principal mints under the project's
 * machine-reveal opt-in; a human mint needs the mint reauthentication
 * ceremony.
 *
 */
export const mintLease = <ThrowOnError extends boolean = false>(options: Options<MintLeaseData, ThrowOnError>) => (options.client ?? client).post<MintLeaseResponses, MintLeaseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/leases',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Show one lease's status and metadata, never the secret.
 */
export const showLease = <ThrowOnError extends boolean = false>(options: Options<ShowLeaseData, ThrowOnError>) => (options.client ?? client).get<ShowLeaseResponses, ShowLeaseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/leases/{lease}',
    ...options
});

/**
 * Extend a lease's validity, bounded by its maximum TTL.
 */
export const renewLease = <ThrowOnError extends boolean = false>(options: Options<RenewLeaseData, ThrowOnError>) => (options.client ?? client).post<RenewLeaseResponses, RenewLeaseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/leases/{lease}/renew',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke a lease; idempotent, an already-gone role is a success.
 */
export const revokeLease = <ThrowOnError extends boolean = false>(options: Options<RevokeLeaseData, ThrowOnError>) => (options.client ?? client).post<RevokeLeaseResponses, RevokeLeaseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/leases/{lease}/revoke',
    ...options
});

/**
 * Settle an uncertain lease by re-probing it at the provider.
 */
export const settleLease = <ThrowOnError extends boolean = false>(options: Options<SettleLeaseData, ThrowOnError>) => (options.client ?? client).post<SettleLeaseResponses, SettleLeaseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/orgs/{org}/projects/{project}/environments/{environment}/leases/{lease}/settle',
    ...options
});
